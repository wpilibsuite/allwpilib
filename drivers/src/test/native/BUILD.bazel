load("@rules_cc//cc:cc_test.bzl", "cc_test")

cc_test(
    name = "native",
    size = "small",
    srcs = glob(["**/*.cpp"]),
    tags = [
        "no-tsan",
        "no-ubsan",
    ],
    deps = [
        "//drivers",
        "//thirdparty/catch2",
    ],
)

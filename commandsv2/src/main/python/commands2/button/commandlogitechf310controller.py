# THIS FILE WAS AUTO-GENERATED BY ./commandsv2/generate_hids.py. DO NOT MODIFY

from typing import Optional, Union

from wpilib import EventLoop, LogitechF310Controller

from .commandgenerichid import CommandGenericHID
from .trigger import Trigger


class CommandLogitechF310Controller:
    """
    A version of :class:`wpilib.LogitechF310Controller` with :class:`.Trigger` factories for command-based.
    """

    _hid: CommandGenericHID
    _controller: LogitechF310Controller

    def __init__(self, hid: Union[int, LogitechF310Controller]):
        """
        Construct an instance of a controller.

        :param hid: The port index on the Driver Station that the controller is plugged into,
                    or the LogitechF310Controller object to use for this controller.
        """
        if isinstance(hid, int):
            self._hid = CommandGenericHID.getCommandGenericHID(hid)
            self._controller = LogitechF310Controller(self._hid.getHID())
        else:
            self._hid = CommandGenericHID(hid.getHID())
            self._controller = hid

    def getHID(self) -> CommandGenericHID:
        """
        Get the underlying CommandGenericHID object.

        :returns: the wrapped CommandGenericHID object
        """
        return self._hid

    def getController(self) -> LogitechF310Controller:
        """
        Get the wrapped controller object.

        :returns: the wrapped controller object
        """
        return self._controller

    def button(
        self,
        button: LogitechF310Controller.Button,
        loop: Optional[EventLoop] = None,
    ) -> Trigger:
        """
        Constructs an event instance around this button's digital signal.

        :param button: the :class:`wpilib.LogitechF310Controller.Button` to read
        :param loop: the event loop instance to attach the event to
        """
        return self._hid.button(button.value, loop)

    def a(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the A button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the A button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.A, loop)

    def b(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the B button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the B button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.B, loop)

    def x(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the X button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the X button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.X, loop)

    def y(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Y button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Y button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.Y, loop)

    def back(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Back button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Back button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.BACK, loop)

    def logitech(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Logitech button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Logitech button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.LOGITECH, loop)

    def start(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Start button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Start button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.START, loop)

    def leftStick(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Left Stick button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Left Stick button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.LEFT_STICK, loop)

    def rightStick(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Right Stick button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Right Stick button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.RIGHT_STICK, loop)

    def leftBumper(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Left Bumper button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Left Bumper button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.LEFT_BUMPER, loop)

    def rightBumper(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Right Bumper button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Right Bumper button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.RIGHT_BUMPER, loop)

    def dpadUp(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Dpad Up button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Dpad Up button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.DPAD_UP, loop)

    def dpadDown(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Dpad Down button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Dpad Down button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.DPAD_DOWN, loop)

    def dpadLeft(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Dpad Left button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Dpad Left button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.DPAD_LEFT, loop)

    def dpadRight(self, loop: Optional[EventLoop] = None) -> Trigger:
        """
        Constructs a Trigger instance around the Dpad Right button's digital signal.

        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance representing the Dpad Right button's digital signal
                  attached to the given loop.
        """
        return self.button(LogitechF310Controller.Button.DPAD_RIGHT, loop)

    def leftTrigger(
        self,
        threshold: float = 0.5,
        loop: Optional[EventLoop] = None,
    ) -> Trigger:
        """
        Constructs a Trigger instance around the Left Trigger axis value. The returned
        Trigger will be true when the axis value is greater than ``threshold``.

        :param threshold: the minimum axis value for the returned Trigger to be true. This value
                          should be in the range [0, 1] where 0 is the unpressed state of the axis.
        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance that is true when the Left Trigger axis exceeds the
                  provided threshold, attached to the given event loop.
        """
        return self.axisGreaterThan(
            LogitechF310Controller.Axis.LEFT_TRIGGER,
            threshold,
            loop,
        )

    def rightTrigger(
        self,
        threshold: float = 0.5,
        loop: Optional[EventLoop] = None,
    ) -> Trigger:
        """
        Constructs a Trigger instance around the Right Trigger axis value. The returned
        Trigger will be true when the axis value is greater than ``threshold``.

        :param threshold: the minimum axis value for the returned Trigger to be true. This value
                          should be in the range [0, 1] where 0 is the unpressed state of the axis.
        :param loop: the event loop instance to attach the Trigger to, defaults
                     to :func:`commands2.CommandScheduler.getDefaultButtonLoop`

        :returns: a Trigger instance that is true when the Right Trigger axis exceeds the
                  provided threshold, attached to the given event loop.
        """
        return self.axisGreaterThan(
            LogitechF310Controller.Axis.RIGHT_TRIGGER,
            threshold,
            loop,
        )

    def axisLessThan(
        self,
        axis: LogitechF310Controller.Axis,
        threshold: float,
        loop: Optional[EventLoop] = None,
    ) -> Trigger:
        """
        Constructs a Trigger instance that is true when the axis value is less than
        ``threshold``, attached to the given loop.

        :param axis: the :class:`wpilib.LogitechF310Controller.Axis` to read
        :param threshold: the value below which this Trigger should return true.
        :param loop: the event loop instance to attach the Trigger to

        :returns: a Trigger instance that is true when the axis value is less than
                  the provided threshold.
        """
        return self._hid.axisLessThan(axis.value, threshold, loop)

    def axisGreaterThan(
        self,
        axis: LogitechF310Controller.Axis,
        threshold: float,
        loop: Optional[EventLoop] = None,
    ) -> Trigger:
        """
        Constructs a Trigger instance that is true when the axis value is greater
        than ``threshold``, attached to the given loop.

        :param axis: the :class:`wpilib.LogitechF310Controller.Axis` to read
        :param threshold: the value above which this Trigger should return true.
        :param loop: the event loop instance to attach the Trigger to.

        :returns: a Trigger instance that is true when the axis value is greater
                  than the provided threshold.
        """
        return self._hid.axisGreaterThan(axis.value, threshold, loop)

    def axisMagnitudeGreaterThan(
        self,
        axis: LogitechF310Controller.Axis,
        threshold: float,
        loop: Optional[EventLoop] = None,
    ) -> Trigger:
        """
        Constructs a Trigger instance that is true when the axis magnitude is
        greater than ``threshold``, attached to the given loop.

        :param axis: the :class:`wpilib.LogitechF310Controller.Axis` to read
        :param threshold: the value above which this Trigger should return true.
        :param loop: the event loop instance to attach the Trigger to.

        :returns: a Trigger instance that is true when the axis magnitude is
                  greater than the provided threshold.
        """
        return self._hid.axisMagnitudeGreaterThan(axis.value, threshold, loop)

    def getAxis(self, axis: LogitechF310Controller.Axis) -> float:
        """
        Get the value of the axis.

        :param axis: the :class:`wpilib.LogitechF310Controller.Axis` to read
        """
        return self._hid.getRawAxis(axis.value)

    def getLeftX(self) -> float:
        """
        Get the Left X value of the controller.

        :returns: the axis value.
        """
        return self._controller.getLeftX()

    def getLeftY(self) -> float:
        """
        Get the Left Y value of the controller.

        :returns: the axis value.
        """
        return self._controller.getLeftY()

    def getRightX(self) -> float:
        """
        Get the Right X value of the controller.

        :returns: the axis value.
        """
        return self._controller.getRightX()

    def getRightY(self) -> float:
        """
        Get the Right Y value of the controller.

        :returns: the axis value.
        """
        return self._controller.getRightY()

    def getLeftTrigger(self) -> float:
        """
        Get the Left Trigger value of the controller.

        :returns: the axis value.
        """
        return self._controller.getLeftTrigger()

    def getRightTrigger(self) -> float:
        """
        Get the Right Trigger value of the controller.

        :returns: the axis value.
        """
        return self._controller.getRightTrigger()

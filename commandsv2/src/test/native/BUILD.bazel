load("@rules_cc//cc:cc_test.bzl", "cc_test")

cc_test(
    name = "native",
    size = "small",
    srcs = glob([
        "**/*.cpp",
        "**/*.hpp",
    ]),
    tags = [
        "no-tsan",
        "no-ubsan",
    ],
    deps = [
        "//commandsv2",
        "//thirdparty/catch2",
    ],
)

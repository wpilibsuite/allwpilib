From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Peter Johnson <johnson.peter@gmail.com>
Date: Sat, 1 Aug 2026 17:02:07 -0700
Subject: [PATCH 34/34] Convert LLVM unit tests to Catch2

---
 llvm/unittests/ADT/DenseMapTest.cpp         | 190 ++++----
 llvm/unittests/ADT/FunctionExtrasTest.cpp   |  15 +-
 llvm/unittests/ADT/PointerIntPairTest.cpp   |  13 +-
 llvm/unittests/ADT/PointerUnionTest.cpp     |  50 ++-
 llvm/unittests/ADT/STLForwardCompatTest.cpp |  65 ++-
 llvm/unittests/ADT/SmallPtrSetTest.cpp      |  30 +-
 llvm/unittests/ADT/SmallSetTest.cpp         |  23 +-
 llvm/unittests/ADT/SmallStringTest.cpp      |  33 +-
 llvm/unittests/ADT/SmallVectorTest.cpp      | 452 +++++++++++---------
 llvm/unittests/Support/ConvertUTFTest.cpp   |  38 +-
 llvm/unittests/Support/EndianTest.cpp       |  61 ++-
 llvm/unittests/Support/ErrnoTest.cpp        |   6 +-
 llvm/unittests/Support/MathExtrasTest.cpp   | 324 +++++++-------
 13 files changed, 665 insertions(+), 635 deletions(-)

diff --git a/llvm/unittests/ADT/DenseMapTest.cpp b/llvm/unittests/ADT/DenseMapTest.cpp
index 0b20004ebb91f519158d244d48aa71f2a23a0dca..fa5d1ef709e818f683a3d0373dd1d0f39a7958e9 100644
--- a/llvm/unittests/ADT/DenseMapTest.cpp
+++ b/llvm/unittests/ADT/DenseMapTest.cpp
@@ -16,14 +16,18 @@
 #include "llvm/ADT/DenseMapInfoVariant.h"
 #include "llvm/ADT/STLForwardCompat.h"
 #include "llvm/ADT/SmallSet.h"
-#include "gmock/gmock.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 #include <map>
 #include <optional>
 #include <set>
 #include <string_view>
 #include <utility>
 #include <variant>
+#include <vector>
 
 using namespace llvm;
 
@@ -111,7 +115,7 @@ std::optional<uint32_t> getTestKey(int i, std::optional<uint32_t> *) {
 // allows all of the map implementations to be tested with shared
 // implementations of helper routines.
 template <typename T>
-class DenseMapTest : public ::testing::Test {
+class DenseMapTest {
 protected:
   T Map;
 
@@ -131,50 +135,43 @@ typename T::key_type *const DenseMapTest<T>::dummy_key_ptr = nullptr;
 template <typename T>
 typename T::mapped_type *const DenseMapTest<T>::dummy_value_ptr = nullptr;
 
-// Register these types for testing.
-// clang-format off
-using DenseMapTestTypes = ::testing::Types<
-    DenseMap<uint32_t, uint32_t>,
-    DenseMap<uint32_t *, uint32_t *>,
-    DenseMap<CtorTester, CtorTester, CtorTesterMapInfo>,
-    DenseMap<EnumClass, uint32_t>,
-    DenseMap<std::optional<uint32_t>, uint32_t>,
-    SmallDenseMap<uint32_t, uint32_t>,
-    SmallDenseMap<uint32_t *, uint32_t *>,
-    SmallDenseMap<CtorTester, CtorTester, 4, CtorTesterMapInfo>,
-    SmallDenseMap<EnumClass, uint32_t>,
-    SmallDenseMap<std::optional<uint32_t>, uint32_t>>;
-// clang-format on
-
-TYPED_TEST_SUITE(DenseMapTest, DenseMapTestTypes, );
+#define WPIUTIL_TEST_TYPES_DenseMapTest \
+  (DenseMap<uint32_t, uint32_t>), (DenseMap<uint32_t*, uint32_t*>), \
+      (DenseMap<CtorTester, CtorTester, CtorTesterMapInfo>), \
+      (DenseMap<EnumClass, uint32_t>), \
+      (DenseMap<std::optional<uint32_t>, uint32_t>), \
+      (SmallDenseMap<uint32_t, uint32_t>), \
+      (SmallDenseMap<uint32_t*, uint32_t*>), \
+      (SmallDenseMap<CtorTester, CtorTester, 4, CtorTesterMapInfo>), \
+      (SmallDenseMap<EnumClass, uint32_t>), \
+      (SmallDenseMap<std::optional<uint32_t>, uint32_t>)
 
 // Empty map tests
-TYPED_TEST(DenseMapTest, EmptyIntMapTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest EmptyIntMapTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   // Size tests
   EXPECT_EQ(0u, this->Map.size());
   EXPECT_TRUE(this->Map.empty());
 
   // Iterator tests
-  EXPECT_TRUE(this->Map.begin() == this->Map.end());
+  EXPECT_EQ(this->Map.begin(), this->Map.end());
 
   // Lookup tests
   EXPECT_FALSE(this->Map.count(this->getKey()));
   EXPECT_FALSE(this->Map.contains(this->getKey()));
-  EXPECT_TRUE(this->Map.find(this->getKey()) == this->Map.end());
-  EXPECT_EQ(typename TypeParam::mapped_type(),
-            this->Map.lookup(this->getKey()));
+  EXPECT_EQ(this->Map.find(this->getKey()), this->Map.end());
+  EXPECT_EQ(typename TestType::mapped_type(), this->Map.lookup(this->getKey()));
 }
 
 // Constant map tests
-TYPED_TEST(DenseMapTest, ConstEmptyMapTest) {
-  const TypeParam &ConstMap = this->Map;
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest ConstEmptyMapTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
+  const TestType &ConstMap = this->Map;
   EXPECT_EQ(0u, ConstMap.size());
   EXPECT_TRUE(ConstMap.empty());
-  EXPECT_TRUE(ConstMap.begin() == ConstMap.end());
+  EXPECT_EQ(ConstMap.begin(), ConstMap.end());
 }
 
 // A map with a single entry
-TYPED_TEST(DenseMapTest, SingleEntryMapTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest SingleEntryMapTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   this->Map[this->getKey()] = this->getValue();
 
   // Size tests
@@ -183,21 +180,21 @@ TYPED_TEST(DenseMapTest, SingleEntryMapTest) {
   EXPECT_FALSE(this->Map.empty());
 
   // Iterator tests
-  typename TypeParam::iterator it = this->Map.begin();
+  typename TestType::iterator it = this->Map.begin();
   EXPECT_EQ(this->getKey(), it->first);
   EXPECT_EQ(this->getValue(), it->second);
   ++it;
-  EXPECT_TRUE(it == this->Map.end());
+  EXPECT_EQ(it, this->Map.end());
 
   // Lookup tests
   EXPECT_TRUE(this->Map.count(this->getKey()));
   EXPECT_TRUE(this->Map.contains(this->getKey()));
-  EXPECT_TRUE(this->Map.find(this->getKey()) == this->Map.begin());
+  EXPECT_EQ(this->Map.find(this->getKey()), this->Map.begin());
   EXPECT_EQ(this->getValue(), this->Map.lookup(this->getKey()));
   EXPECT_EQ(this->getValue(), this->Map[this->getKey()]);
 }
 
-TYPED_TEST(DenseMapTest, AtTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest AtTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   this->Map[this->getKey(0)] = this->getValue(0);
   this->Map[this->getKey(1)] = this->getValue(1);
   this->Map[this->getKey(2)] = this->getValue(2);
@@ -215,56 +212,56 @@ TYPED_TEST(DenseMapTest, AtTest) {
 }
 
 // Test clear() method
-TYPED_TEST(DenseMapTest, ClearTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest ClearTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   this->Map[this->getKey()] = this->getValue();
   this->Map.clear();
 
   EXPECT_EQ(0u, this->Map.size());
   EXPECT_TRUE(this->Map.empty());
-  EXPECT_TRUE(this->Map.begin() == this->Map.end());
+  EXPECT_EQ(this->Map.begin(), this->Map.end());
 }
 
 // Test erase(iterator) method
-TYPED_TEST(DenseMapTest, EraseTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest EraseTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   this->Map[this->getKey()] = this->getValue();
   this->Map.erase(this->Map.begin());
 
   EXPECT_EQ(0u, this->Map.size());
   EXPECT_TRUE(this->Map.empty());
-  EXPECT_TRUE(this->Map.begin() == this->Map.end());
+  EXPECT_EQ(this->Map.begin(), this->Map.end());
 }
 
 // Test erase(value) method
-TYPED_TEST(DenseMapTest, EraseTest2) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest EraseTest2", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   this->Map[this->getKey()] = this->getValue();
   this->Map.erase(this->getKey());
 
   EXPECT_EQ(0u, this->Map.size());
   EXPECT_TRUE(this->Map.empty());
-  EXPECT_TRUE(this->Map.begin() == this->Map.end());
+  EXPECT_EQ(this->Map.begin(), this->Map.end());
 }
 
 // Test insert() method
-TYPED_TEST(DenseMapTest, InsertTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest InsertTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   this->Map.insert(std::make_pair(this->getKey(), this->getValue()));
   EXPECT_EQ(1u, this->Map.size());
   EXPECT_EQ(this->getValue(), this->Map[this->getKey()]);
 }
 
 // Test copy constructor method
-TYPED_TEST(DenseMapTest, CopyConstructorTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest CopyConstructorTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   this->Map[this->getKey()] = this->getValue();
-  TypeParam copyMap(this->Map);
+  TestType copyMap(this->Map);
 
   EXPECT_EQ(1u, copyMap.size());
   EXPECT_EQ(this->getValue(), copyMap[this->getKey()]);
 }
 
 // Test copy constructor method where SmallDenseMap isn't small.
-TYPED_TEST(DenseMapTest, CopyConstructorNotSmallTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest CopyConstructorNotSmallTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   for (int Key = 0; Key < 5; ++Key)
     this->Map[this->getKey(Key)] = this->getValue(Key);
-  TypeParam copyMap(this->Map);
+  TestType copyMap(this->Map);
 
   EXPECT_EQ(5u, copyMap.size());
   for (int Key = 0; Key < 5; ++Key)
@@ -272,18 +269,18 @@ TYPED_TEST(DenseMapTest, CopyConstructorNotSmallTest) {
 }
 
 // Test range constructors.
-TYPED_TEST(DenseMapTest, RangeConstructorTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest RangeConstructorTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   using KeyAndValue =
-      std::pair<typename TypeParam::key_type, typename TypeParam::mapped_type>;
+      std::pair<typename TestType::key_type, typename TestType::mapped_type>;
   KeyAndValue PlainArray[] = {{this->getKey(0), this->getValue(0)},
                               {this->getKey(1), this->getValue(1)}};
 
-  TypeParam MapFromRange(llvm::from_range, PlainArray);
+  TestType MapFromRange(llvm::from_range, PlainArray);
   EXPECT_EQ(2u, MapFromRange.size());
   EXPECT_EQ(this->getValue(0), MapFromRange[this->getKey(0)]);
   EXPECT_EQ(this->getValue(1), MapFromRange[this->getKey(1)]);
 
-  TypeParam MapFromInitList({{this->getKey(0), this->getValue(1)},
+  TestType MapFromInitList({{this->getKey(0), this->getValue(1)},
                              {this->getKey(1), this->getValue(2)}});
   EXPECT_EQ(2u, MapFromInitList.size());
   EXPECT_EQ(this->getValue(1), MapFromInitList[this->getKey(0)]);
@@ -291,56 +288,56 @@ TYPED_TEST(DenseMapTest, RangeConstructorTest) {
 }
 
 // Test copying from a default-constructed map.
-TYPED_TEST(DenseMapTest, CopyConstructorFromDefaultTest) {
-  TypeParam copyMap(this->Map);
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest CopyConstructorFromDefaultTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
+  TestType copyMap(this->Map);
 
   EXPECT_TRUE(copyMap.empty());
 }
 
 // Test copying from an empty map where SmallDenseMap isn't small.
-TYPED_TEST(DenseMapTest, CopyConstructorFromEmptyTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest CopyConstructorFromEmptyTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   for (int Key = 0; Key < 5; ++Key)
     this->Map[this->getKey(Key)] = this->getValue(Key);
   this->Map.clear();
-  TypeParam copyMap(this->Map);
+  TestType copyMap(this->Map);
 
   EXPECT_TRUE(copyMap.empty());
 }
 
 // Test assignment operator method
-TYPED_TEST(DenseMapTest, AssignmentTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest AssignmentTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   this->Map[this->getKey()] = this->getValue();
-  TypeParam copyMap = this->Map;
+  TestType copyMap = this->Map;
 
   EXPECT_EQ(1u, copyMap.size());
   EXPECT_EQ(this->getValue(), copyMap[this->getKey()]);
 
   // test self-assignment.
-  copyMap = static_cast<TypeParam &>(copyMap);
+  copyMap = static_cast<TestType &>(copyMap);
   EXPECT_EQ(1u, copyMap.size());
   EXPECT_EQ(this->getValue(), copyMap[this->getKey()]);
 }
 
-TYPED_TEST(DenseMapTest, AssignmentTestNotSmall) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest AssignmentTestNotSmall", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   for (int Key = 0; Key < 5; ++Key)
     this->Map[this->getKey(Key)] = this->getValue(Key);
-  TypeParam copyMap = this->Map;
+  TestType copyMap = this->Map;
 
   EXPECT_EQ(5u, copyMap.size());
   for (int Key = 0; Key < 5; ++Key)
     EXPECT_EQ(this->getValue(Key), copyMap[this->getKey(Key)]);
 
   // test self-assignment.
-  copyMap = static_cast<TypeParam &>(copyMap);
+  copyMap = static_cast<TestType &>(copyMap);
   EXPECT_EQ(5u, copyMap.size());
   for (int Key = 0; Key < 5; ++Key)
     EXPECT_EQ(this->getValue(Key), copyMap[this->getKey(Key)]);
 }
 
 // Test swap method
-TYPED_TEST(DenseMapTest, SwapTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest SwapTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   this->Map[this->getKey()] = this->getValue();
-  TypeParam otherMap;
+  TestType otherMap;
 
   this->Map.swap(otherMap);
   EXPECT_EQ(0u, this->Map.size());
@@ -374,9 +371,9 @@ TYPED_TEST(DenseMapTest, SwapTest) {
 }
 
 // A more complex iteration test
-TYPED_TEST(DenseMapTest, IterationTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest IterationTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   bool visited[100];
-  std::map<typename TypeParam::key_type, unsigned> visitedIndex;
+  std::map<typename TestType::key_type, unsigned> visitedIndex;
 
   // Insert 100 numbers into the map
   for (int i = 0; i < 100; ++i) {
@@ -387,25 +384,27 @@ TYPED_TEST(DenseMapTest, IterationTest) {
   }
 
   // Iterate over all numbers and mark each one found.
-  for (typename TypeParam::iterator it = this->Map.begin();
+  for (typename TestType::iterator it = this->Map.begin();
        it != this->Map.end(); ++it)
     visited[visitedIndex[it->first]] = true;
 
   // Ensure every number was visited.
-  for (int i = 0; i < 100; ++i)
-    ASSERT_TRUE(visited[i]) << "Entry #" << i << " was never visited";
+  for (int i = 0; i < 100; ++i) {
+    SCOPED_TRACE("Entry #" << i << " was never visited");
+    ASSERT_TRUE(visited[i]);
+  }
 }
 
 // const_iterator test
-TYPED_TEST(DenseMapTest, ConstIteratorTest) {
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest ConstIteratorTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
   // Check conversion from iterator to const_iterator.
-  typename TypeParam::iterator it = this->Map.begin();
-  typename TypeParam::const_iterator cit(it);
-  EXPECT_TRUE(it == cit);
+  typename TestType::iterator it = this->Map.begin();
+  typename TestType::const_iterator cit(it);
+  EXPECT_EQ(it, cit);
 
   // Check copying of const_iterators.
-  typename TypeParam::const_iterator cit2(cit);
-  EXPECT_TRUE(cit == cit2);
+  typename TestType::const_iterator cit2(cit);
+  EXPECT_EQ(cit, cit2);
 }
 
 // TYPED_TEST below cycles through different types.  We define UniversalSmallSet
@@ -415,9 +414,9 @@ template <typename T, unsigned N>
 using UniversalSmallSet =
     std::conditional_t<std::is_pointer_v<T>, SmallPtrSet<T, N>, SmallSet<T, N>>;
 
-TYPED_TEST(DenseMapTest, KeysValuesIterator) {
-  UniversalSmallSet<typename TypeParam::key_type, 10> Keys;
-  UniversalSmallSet<typename TypeParam::mapped_type, 10> Values;
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest KeysValuesIterator", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
+  UniversalSmallSet<typename TestType::key_type, 10> Keys;
+  UniversalSmallSet<typename TestType::mapped_type, 10> Values;
   for (int I = 0; I < 10; ++I) {
     auto K = this->getKey(I);
     auto V = this->getValue(I);
@@ -426,8 +425,8 @@ TYPED_TEST(DenseMapTest, KeysValuesIterator) {
     this->Map[K] = V;
   }
 
-  UniversalSmallSet<typename TypeParam::key_type, 10> ActualKeys;
-  UniversalSmallSet<typename TypeParam::mapped_type, 10> ActualValues;
+  UniversalSmallSet<typename TestType::key_type, 10> ActualKeys;
+  UniversalSmallSet<typename TestType::mapped_type, 10> ActualValues;
   for (auto K : this->Map.keys())
     ActualKeys.insert(K);
   for (auto V : this->Map.values())
@@ -437,9 +436,9 @@ TYPED_TEST(DenseMapTest, KeysValuesIterator) {
   EXPECT_EQ(Values, ActualValues);
 }
 
-TYPED_TEST(DenseMapTest, ConstKeysValuesIterator) {
-  UniversalSmallSet<typename TypeParam::key_type, 10> Keys;
-  UniversalSmallSet<typename TypeParam::mapped_type, 10> Values;
+TEMPLATE_TEST_CASE_METHOD(DenseMapTest, "DenseMapTest ConstKeysValuesIterator", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DenseMapTest) {
+  UniversalSmallSet<typename TestType::key_type, 10> Keys;
+  UniversalSmallSet<typename TestType::mapped_type, 10> Values;
   for (int I = 0; I < 10; ++I) {
     auto K = this->getKey(I);
     auto V = this->getValue(I);
@@ -448,9 +447,9 @@ TYPED_TEST(DenseMapTest, ConstKeysValuesIterator) {
     this->Map[K] = V;
   }
 
-  const TypeParam &ConstMap = this->Map;
-  UniversalSmallSet<typename TypeParam::key_type, 10> ActualKeys;
-  UniversalSmallSet<typename TypeParam::mapped_type, 10> ActualValues;
+  const TestType &ConstMap = this->Map;
+  UniversalSmallSet<typename TestType::key_type, 10> ActualKeys;
+  UniversalSmallSet<typename TestType::mapped_type, 10> ActualValues;
   for (auto K : ConstMap.keys())
     ActualKeys.insert(K);
   for (auto V : ConstMap.values())
@@ -487,8 +486,8 @@ TEST(DenseMapCustomTest, InsertRange) {
   M.insert_range(InputVals);
 
   EXPECT_EQ(M.size(), 2u);
-  EXPECT_THAT(M, testing::UnorderedElementsAre(testing::Pair(0, 0),
-                                               testing::Pair(1, 2)));
+  CHECK_THAT(M, Catch::Matchers::UnorderedRangeEquals(
+                    {std::pair{0, 0}, std::pair{1, 2}}));
 }
 
 TEST(SmallDenseMapCustomTest, InsertRange) {
@@ -498,8 +497,8 @@ TEST(SmallDenseMapCustomTest, InsertRange) {
   M.insert_range(InputVals);
 
   EXPECT_EQ(M.size(), 2u);
-  EXPECT_THAT(M, testing::UnorderedElementsAre(testing::Pair(0, 0),
-                                               testing::Pair(1, 2)));
+  CHECK_THAT(M, Catch::Matchers::UnorderedRangeEquals(
+                    {std::pair{0, 0}, std::pair{1, 2}}));
 }
 
 // Test for the default minimum size of a DenseMap
@@ -740,22 +739,22 @@ TEST(DenseMapCustomTest, FindAsTest) {
   EXPECT_EQ(1u, map.find(0)->second);
   EXPECT_EQ(2u, map.find(1)->second);
   EXPECT_EQ(3u, map.find(2)->second);
-  EXPECT_TRUE(map.find(3) == map.end());
+  EXPECT_EQ(map.find(3), map.end());
 
   // find_as() tests
   EXPECT_EQ(1u, map.find_as("a")->second);
   EXPECT_EQ(2u, map.find_as("b")->second);
   EXPECT_EQ(3u, map.find_as("c")->second);
-  EXPECT_TRUE(map.find_as("d") == map.end());
+  EXPECT_EQ(map.find_as("d"), map.end());
 }
 
 TEST(DenseMapCustomTest, SmallDenseMapFromRange) {
   std::pair<int, std::string_view> PlainArray[] = {{0, "0"}, {1, "1"}, {2, "2"}};
   SmallDenseMap<int, std::string_view> M(llvm::from_range, PlainArray);
   EXPECT_EQ(3u, M.size());
-  using testing::Pair;
-  EXPECT_THAT(M, testing::UnorderedElementsAre(Pair(0, "0"), Pair(1, "1"),
-                                               Pair(2, "2")));
+  CHECK_THAT(
+      M, Catch::Matchers::UnorderedRangeEquals(
+             std::vector<std::pair<int, std::string_view>>{{0, "0"}, {1, "1"}, {2, "2"}}));
 }
 
 TEST(DenseMapCustomTest, SmallDenseMapInitializerList) {
@@ -797,7 +796,7 @@ TEST(DenseMapCustomTest, SmallDenseMapGrowTest) {
   // SmallDenseMap which led to a map with num elements == small capacity not
   // having an empty bucket any more.  Finding an element not in the map would
   // therefore never terminate.
-  EXPECT_TRUE(map.find(32) == map.end());
+  EXPECT_EQ(map.find(32), map.end());
 }
 
 TEST(DenseMapCustomTest, LargeSmallDenseMapCompaction) {
@@ -815,7 +814,7 @@ TEST(DenseMapCustomTest, LargeSmallDenseMapCompaction) {
   EXPECT_EQ(33u, map.size());
   // Similar to the previous test, check for a non-existing element, as an
   // indirect check that tombstones have been removed.
-  EXPECT_TRUE(map.find(0) == map.end());
+  EXPECT_EQ(map.find(0), map.end());
 }
 
 TEST(DenseMapCustomTest, SmallDenseMapWithNumBucketsNonPowerOf2) {
@@ -943,12 +942,11 @@ TEST(DenseMapCustomTest, VariantSupport) {
   };
   Map.try_emplace(Keys[0], 0);
   Map.try_emplace(Keys[1], 1);
-  EXPECT_THAT(Map, testing::SizeIs(2));
-  EXPECT_NE(DenseMapInfo<variant>::getHashValue(Keys[0]),
-            DenseMapInfo<variant>::getHashValue(Keys[1]));
+  EXPECT_EQ(Map.size(), 2u);
+  EXPECT_NE(DenseMapInfo<variant>::getHashValue(Keys[0]), DenseMapInfo<variant>::getHashValue(Keys[1]));
   // Check that isEqual dispatches to isEqual of underlying type, and not to
   // operator==.
-  EXPECT_FALSE(DenseMapInfo<variant>::isEqual(Keys[2], Keys[2]));
+  EXPECT_FALSE(DenseMapInfo < variant>::isEqual(Keys[2], Keys[2]));
 }
 
 TEST(DenseMapCustomTest, InitSize) {
diff --git a/llvm/unittests/ADT/FunctionExtrasTest.cpp b/llvm/unittests/ADT/FunctionExtrasTest.cpp
index 3a2008adb0697757c68af8ad462b9af716c456a6..f2e41fcd9f18f3b789a8cfa2f70cf7380de25a21 100644
--- a/llvm/unittests/ADT/FunctionExtrasTest.cpp
+++ b/llvm/unittests/ADT/FunctionExtrasTest.cpp
@@ -8,7 +8,11 @@
 
 #include "llvm/ADT/FunctionExtras.h"
 #include "CountCopyAndMove.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 
 #include <memory>
 
@@ -53,7 +57,7 @@ TEST(UniqueFunctionTest, Basic) {
       };
   std::unique_ptr<int> Ptr{new int(13)};
   EXPECT_EQ(ReadAndDeallocByRef(std::move(Ptr)), 13);
-  EXPECT_FALSE((bool)Ptr);
+  EXPECT_FALSE(static_cast<bool>(Ptr));
 
   // Make sure we can pass a move-only temporary as opposed to a local variable.
   EXPECT_EQ(ReadAndDeallocByRef(std::unique_ptr<int>(new int(42))), 42);
@@ -67,7 +71,7 @@ TEST(UniqueFunctionTest, Basic) {
       };
   Ptr.reset(new int(13));
   EXPECT_EQ(ReadAndDeallocByVal(std::move(Ptr)), 13);
-  EXPECT_FALSE((bool)Ptr);
+  EXPECT_FALSE(static_cast<bool>(Ptr));
 
   EXPECT_EQ(ReadAndDeallocByVal(std::unique_ptr<int>(new int(42))), 42);
 }
@@ -324,7 +328,7 @@ TEST(UniqueFunctionTest, InlineStorageWorks) {
                    sizeof(unique_function<void(void *)>);
         // Make sure the address of the captured pointer lies somewhere within
         // the unique_function object.
-        EXPECT_TRUE(mid >= beg && mid < end);
+        EXPECT_TRUE((mid >= beg && mid < end));
       }};
   UniqueFunctionWithInlineStorage(&UniqueFunctionWithInlineStorage);
 }
@@ -345,8 +349,7 @@ TEST(UniqueFunctionTest, MovedFromStateIsDestroyedCorrectly) {
     unique_function<void()> CapturingFunctionMoved{
         std::move(CapturingFunction)};
   }
-  EXPECT_EQ(CountCopyAndMove::TotalConstructions(),
-            CountCopyAndMove::Destructions);
+  EXPECT_EQ(CountCopyAndMove::TotalConstructions(), CountCopyAndMove::Destructions);
 }
 
 } // anonymous namespace
diff --git a/llvm/unittests/ADT/PointerIntPairTest.cpp b/llvm/unittests/ADT/PointerIntPairTest.cpp
index 0e1b87c0b3141799161e6649d4e5be96429dbf5e..b6c8e6e6b8517a46841e4fd3a6d75419cab0ba7e 100644
--- a/llvm/unittests/ADT/PointerIntPairTest.cpp
+++ b/llvm/unittests/ADT/PointerIntPairTest.cpp
@@ -7,7 +7,11 @@
 //===----------------------------------------------------------------------===//
 
 #include "llvm/ADT/PointerIntPair.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 #include <limits>
 using namespace llvm;
 
@@ -94,15 +98,14 @@ struct FixnumPointerTraits {
 TEST(PointerIntPairTest, ManyUnusedBits) {
 
   PointerIntPair<Fixnum31, 1, bool, FixnumPointerTraits> pair;
-  EXPECT_EQ((uintptr_t)0, pair.getPointer().Value);
+  EXPECT_EQ(static_cast<uintptr_t>(0), pair.getPointer().Value);
   EXPECT_FALSE(pair.getInt());
 
   pair.setPointerAndInt({ 0x7FFFFFFF }, true );
-  EXPECT_EQ((uintptr_t)0x7FFFFFFF, pair.getPointer().Value);
+  EXPECT_EQ(static_cast<uintptr_t>(0x7FFFFFFF), pair.getPointer().Value);
   EXPECT_TRUE(pair.getInt());
 
-  EXPECT_EQ(FixnumPointerTraits::NumLowBitsAvailable - 1,
-            (int)PointerLikeTypeTraits<decltype(pair)>::NumLowBitsAvailable);
+  EXPECT_EQ(FixnumPointerTraits::NumLowBitsAvailable - 1, (int)PointerLikeTypeTraits<decltype(pair)>::NumLowBitsAvailable);
 
   static_assert(std::is_trivially_copyable_v<
                     PointerIntPair<Fixnum31, 1, bool, FixnumPointerTraits>>,
diff --git a/llvm/unittests/ADT/PointerUnionTest.cpp b/llvm/unittests/ADT/PointerUnionTest.cpp
index d8ac3aed76da28a2406d3fdb3876c924faba9366..e2e96e97e5fe805407b51c7c7e22602b31090cbe 100644
--- a/llvm/unittests/ADT/PointerUnionTest.cpp
+++ b/llvm/unittests/ADT/PointerUnionTest.cpp
@@ -7,7 +7,11 @@
 //===----------------------------------------------------------------------===//
 
 #include "llvm/ADT/PointerUnion.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 using namespace llvm;
 
 namespace {
@@ -16,7 +20,7 @@ using PU = PointerUnion<int *, float *>;
 using PU3 = PointerUnion<int *, float *, long long *>;
 using PU4 = PointerUnion<int *, float *, long long *, double *>;
 
-struct PointerUnionTest : public testing::Test {
+struct PointerUnionTest {
   float f;
   int i;
   double d;
@@ -35,27 +39,27 @@ struct PointerUnionTest : public testing::Test {
 };
 
 TEST_F(PointerUnionTest, Comparison) {
-  EXPECT_TRUE(a == a);
+  EXPECT_EQ(a, a);
   EXPECT_FALSE(a != a);
-  EXPECT_TRUE(a != b);
+  EXPECT_NE(a, b);
   EXPECT_FALSE(a == b);
-  EXPECT_TRUE(b == c);
+  EXPECT_EQ(b, c);
   EXPECT_FALSE(b != c);
-  EXPECT_TRUE(b != n);
+  EXPECT_NE(b, n);
   EXPECT_FALSE(b == n);
-  EXPECT_TRUE(i3 == i3);
+  EXPECT_EQ(i3, i3);
   EXPECT_FALSE(i3 != i3);
-  EXPECT_TRUE(i3 != f3);
-  EXPECT_TRUE(f3 != l3);
-  EXPECT_TRUE(i4 == i4);
+  EXPECT_NE(i3, f3);
+  EXPECT_NE(f3, l3);
+  EXPECT_EQ(i4, i4);
   EXPECT_FALSE(i4 != i4);
-  EXPECT_TRUE(i4 != f4);
-  EXPECT_TRUE(i4 != l4);
-  EXPECT_TRUE(f4 != l4);
-  EXPECT_TRUE(l4 != d4);
-  EXPECT_TRUE(i4null != f4null);
-  EXPECT_TRUE(i4null != l4null);
-  EXPECT_TRUE(i4null != d4null);
+  EXPECT_NE(i4, f4);
+  EXPECT_NE(i4, l4);
+  EXPECT_NE(f4, l4);
+  EXPECT_NE(l4, d4);
+  EXPECT_NE(i4null, f4null);
+  EXPECT_NE(i4null, l4null);
+  EXPECT_NE(i4null, d4null);
 }
 
 TEST_F(PointerUnionTest, Null) {
@@ -66,8 +70,8 @@ TEST_F(PointerUnionTest, Null) {
   EXPECT_FALSE(!b);
   EXPECT_TRUE(!n);
   // workaround an issue with EXPECT macros and explicit bool
-  EXPECT_TRUE((bool)a);
-  EXPECT_TRUE((bool)b);
+  EXPECT_TRUE(static_cast<bool>(a));
+  EXPECT_TRUE(static_cast<bool>(b));
   EXPECT_FALSE(n);
 
   EXPECT_NE(n, b);
@@ -147,13 +151,13 @@ TEST_F(PointerUnionTest, ManyElements) {
   EXPECT_TRUE(isa<Aligned<7> *>(a));
   EXPECT_EQ(dyn_cast_if_present<Aligned<7> *>(a), &a7);
 
-  EXPECT_TRUE(a == PU8(&a7));
-  EXPECT_TRUE(a != PU8(&a0));
+  EXPECT_EQ(a, PU8(&a7));
+  EXPECT_NE(a, PU8(&a0));
 }
 
 TEST_F(PointerUnionTest, GetAddrOfPtr1) {
-  EXPECT_TRUE((void *)b.getAddrOfPtr1() == (void *)&b);
-  EXPECT_TRUE((void *)n.getAddrOfPtr1() == (void *)&n);
+  EXPECT_EQ(static_cast<void*>(b.getAddrOfPtr1()), static_cast<void*>(&b));
+  EXPECT_EQ(static_cast<void*>(n.getAddrOfPtr1()), static_cast<void*>(&n));
 }
 
 TEST_F(PointerUnionTest, NewCastInfra) {
diff --git a/llvm/unittests/ADT/STLForwardCompatTest.cpp b/llvm/unittests/ADT/STLForwardCompatTest.cpp
index d0092fdb52b01ce8c237f2d8a6e9378b80ad3810..50ff72e8b8fb70db85f11271c4af855a62e69b34 100644
--- a/llvm/unittests/ADT/STLForwardCompatTest.cpp
+++ b/llvm/unittests/ADT/STLForwardCompatTest.cpp
@@ -8,7 +8,11 @@
 
 #include "llvm/ADT/STLForwardCompat.h"
 #include "CountCopyAndMove.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 
 #include <optional>
 #include <type_traits>
@@ -17,39 +21,28 @@
 namespace {
 
 template <typename T>
-class STLForwardCompatRemoveCVRefTest : public ::testing::Test {};
-
-using STLForwardCompatRemoveCVRefTestTypes = ::testing::Types<
-    // clang-format off
-    std::pair<int, int>,
-    std::pair<int &, int>,
-    std::pair<const int, int>,
-    std::pair<volatile int, int>,
-    std::pair<const volatile int &, int>,
-    std::pair<int *, int *>,
-    std::pair<int *const, int *>,
-    std::pair<const int *, const int *>,
-    std::pair<int *&, int *>
-    // clang-format on
-    >;
-
-TYPED_TEST_SUITE(STLForwardCompatRemoveCVRefTest,
-                 STLForwardCompatRemoveCVRefTestTypes, );
-
-TYPED_TEST(STLForwardCompatRemoveCVRefTest, RemoveCVRef) {
-  using From = typename TypeParam::first_type;
-  using To = typename TypeParam::second_type;
-  EXPECT_TRUE(
-      (std::is_same<typename llvm::remove_cvref<From>::type, To>::value));
+class STLForwardCompatRemoveCVRefTest {};
+
+#define WPIUTIL_TEST_TYPES_STLForwardCompatRemoveCVRefTest \
+  (std::pair<int, int>), (std::pair<int&, int>), \
+      (std::pair<const int, int>), (std::pair<volatile int, int>), \
+      (std::pair<const volatile int&, int>), (std::pair<int*, int*>), \
+      (std::pair<int* const, int*>), (std::pair<const int*, const int*>), \
+      (std::pair<int*&, int*>)
+
+TEMPLATE_TEST_CASE_METHOD(STLForwardCompatRemoveCVRefTest, "STLForwardCompatRemoveCVRefTest RemoveCVRef", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_STLForwardCompatRemoveCVRefTest) {
+  using From = typename TestType::first_type;
+  using To = typename TestType::second_type;
+  CHECK(std::is_same < typename llvm::remove_cvref<From>::type, To>::value);
 }
 
-TYPED_TEST(STLForwardCompatRemoveCVRefTest, RemoveCVRefT) {
-  using From = typename TypeParam::first_type;
-  EXPECT_TRUE((std::is_same<typename llvm::remove_cvref<From>::type,
-                            llvm::remove_cvref_t<From>>::value));
+TEMPLATE_TEST_CASE_METHOD(STLForwardCompatRemoveCVRefTest, "STLForwardCompatRemoveCVRefTest RemoveCVRefT", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_STLForwardCompatRemoveCVRefTest) {
+  using From = typename TestType::first_type;
+  CHECK(std::is_same < typename llvm::remove_cvref<From>::type,
+                            llvm::remove_cvref_t<From>>::value);
 }
 
-template <typename T> class TypeIdentityTest : public ::testing::Test {
+template <typename T> class TypeIdentityTest {
 public:
   using TypeIdentity = llvm::type_identity<T>;
 };
@@ -57,15 +50,11 @@ public:
 struct A {
   struct B {};
 };
-using TypeIdentityTestTypes =
-    ::testing::Types<int, volatile int, A, const A::B>;
+#define WPIUTIL_TEST_TYPES_TypeIdentityTest int, volatile int, A, const A::B
 
-TYPED_TEST_SUITE(TypeIdentityTest, TypeIdentityTestTypes, /*NameGenerator*/);
-
-TYPED_TEST(TypeIdentityTest, Identity) {
-  // TestFixture is the instantiated TypeIdentityTest.
-  EXPECT_TRUE(
-      (std::is_same_v<TypeParam, typename TestFixture::TypeIdentity::type>));
+TEMPLATE_TEST_CASE_METHOD(TypeIdentityTest, "TypeIdentityTest Identity", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_TypeIdentityTest) {
+  // TypeIdentityTest<TestType> is the instantiated TypeIdentityTest.
+  CHECK(std::is_same_v < TestType, typename TypeIdentityTest<TestType>::TypeIdentity::type>);
 }
 
 TEST(TransformTest, TransformStd) {
diff --git a/llvm/unittests/ADT/SmallPtrSetTest.cpp b/llvm/unittests/ADT/SmallPtrSetTest.cpp
index 33d167cf87e588d46682916888eaf81713854046..444420e0eb1daca268d4b699d9f32c1eb1c0275b 100644
--- a/llvm/unittests/ADT/SmallPtrSetTest.cpp
+++ b/llvm/unittests/ADT/SmallPtrSetTest.cpp
@@ -13,13 +13,16 @@
 #include "llvm/ADT/SmallPtrSet.h"
 #include "llvm/ADT/PointerIntPair.h"
 #include "llvm/Support/PointerLikeTypeTraits.h"
-#include "gmock/gmock.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 
 #include <algorithm>
 
 using namespace llvm;
-using testing::UnorderedElementsAre;
+using Catch::Matchers::UnorderedRangeEquals;
 
 TEST(SmallPtrSetTest, Assignment) {
   int buf[8];
@@ -71,7 +74,7 @@ TEST(SmallPtrSetTest, GrowthTest) {
       (**I)++;
   EXPECT_EQ(4, i);
   for(i=0; i<8; ++i)
-      EXPECT_EQ(i<4?1:0,buf[i]);
+      EXPECT_EQ((i < 4 ? 1 : 0), buf[i]);
 
   s.insert(&buf[4]);
   s.insert(&buf[5]);
@@ -93,7 +96,7 @@ TEST(SmallPtrSetTest, GrowthTest) {
       (**I)++;
   EXPECT_EQ(4, i);
   for(i=0; i<8; ++i)
-      EXPECT_EQ(i<4?3:1,buf[i]);
+      EXPECT_EQ((i < 4 ? 3 : 1), buf[i]);
 
   s.clear();
   for(i=0; i<8; ++i) buf[i]=0;
@@ -102,7 +105,7 @@ TEST(SmallPtrSetTest, GrowthTest) {
   for(iter I=s.begin(), E=s.end(); I!=E; ++I, ++i)
       (**I)++;
   for(i=0; i<8; ++i)
-      EXPECT_EQ(1,buf[i]);
+      EXPECT_EQ(1, buf[i]);
 }
 
 TEST(SmallPtrSetTest, CopyAndMoveTest) {
@@ -418,7 +421,7 @@ TEST(SmallPtrSetTest, CtorRange) {
   int V2 = 2;
   int *Args[] = {&V2, &V0, &V1};
   SmallPtrSet<int *, 4> Set(llvm::from_range, Args);
-  EXPECT_THAT(Set, UnorderedElementsAre(&V0, &V1, &V2));
+  CHECK_THAT(Set, UnorderedRangeEquals({&V0, &V1, &V2}));
 }
 
 TEST(SmallPtrSetTest, InsertRange) {
@@ -428,7 +431,7 @@ TEST(SmallPtrSetTest, InsertRange) {
   SmallPtrSet<int *, 4> Set;
   int *Args[] = {&V2, &V0, &V1};
   Set.insert_range(Args);
-  EXPECT_THAT(Set, UnorderedElementsAre(&V0, &V1, &V2));
+  CHECK_THAT(Set, UnorderedRangeEquals({&V0, &V1, &V2}));
 }
 
 TEST(SmallPtrSetTest, Reserve) {
@@ -450,16 +453,16 @@ TEST(SmallPtrSetTest, Reserve) {
   Set.reserve(4);
   EXPECT_EQ(Set.capacity(), 4u);
   EXPECT_EQ(Set.size(), 4u);
-  EXPECT_THAT(Set,
-              UnorderedElementsAre(&Vals[0], &Vals[1], &Vals[2], &Vals[3]));
+  CHECK_THAT(Set,
+             UnorderedRangeEquals({&Vals[0], &Vals[1], &Vals[2], &Vals[3]}));
 
   // Reserving further should lead to a reallocation. And matching the existing
   // insertion approach, we immediately allocate up to 128 elements.
   Set.reserve(5);
   EXPECT_EQ(Set.capacity(), 128u);
   EXPECT_EQ(Set.size(), 4u);
-  EXPECT_THAT(Set,
-              UnorderedElementsAre(&Vals[0], &Vals[1], &Vals[2], &Vals[3]));
+  CHECK_THAT(Set,
+             UnorderedRangeEquals({&Vals[0], &Vals[1], &Vals[2], &Vals[3]}));
 
   // And we should be able to insert another two or three elements without
   // reallocating.
@@ -475,7 +478,8 @@ TEST(SmallPtrSetTest, Reserve) {
   Set.reserve(0);
   EXPECT_EQ(Set.capacity(), 128u);
   EXPECT_EQ(Set.size(), 6u);
-  EXPECT_THAT(Set, UnorderedElementsAre(&Vals[0], &Vals[1], &Vals[2], &Vals[3], &Vals[4], &Vals[5]));
+  CHECK_THAT(Set, UnorderedRangeEquals({&Vals[0], &Vals[1], &Vals[2],
+                                        &Vals[3], &Vals[4], &Vals[5]}));
 
   // Reserving 192 should result in 256 buckets.
   Set.reserve(192);
diff --git a/llvm/unittests/ADT/SmallSetTest.cpp b/llvm/unittests/ADT/SmallSetTest.cpp
index 7cb7e6c9d8482ed185845c6b09e4b91355d9b413..e7fcb5aa502c52af3bd4338cdeb4eb6fe92a7d5a 100644
--- a/llvm/unittests/ADT/SmallSetTest.cpp
+++ b/llvm/unittests/ADT/SmallSetTest.cpp
@@ -11,8 +11,11 @@
 //===----------------------------------------------------------------------===//
 
 #include "llvm/ADT/SmallSet.h"
-#include "gmock/gmock.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 #include <algorithm>
 #include <string>
 
@@ -21,20 +24,20 @@ using namespace llvm;
 TEST(SmallSetTest, ConstructorIteratorPair) {
   std::initializer_list<int> L = {1, 2, 3, 4, 5};
   SmallSet<int, 4> S(std::begin(L), std::end(L));
-  EXPECT_THAT(S, testing::UnorderedElementsAreArray(L));
+  CHECK_THAT(S, Catch::Matchers::UnorderedRangeEquals(L));
 }
 
 TEST(SmallSet, ConstructorInitializerList) {
   std::initializer_list<int> L = {1, 2, 3, 4, 5};
   SmallSet<int, 4> S = {1, 2, 3, 4, 5};
-  EXPECT_THAT(S, testing::UnorderedElementsAreArray(L));
+  CHECK_THAT(S, Catch::Matchers::UnorderedRangeEquals(L));
 }
 
 TEST(SmallSet, CopyConstructor) {
   SmallSet<int, 4> S = {1, 2, 3};
   SmallSet<int, 4> T = S;
 
-  EXPECT_THAT(S, testing::ContainerEq(T));
+  CHECK_THAT(S, Catch::Matchers::RangeEquals(T));
 }
 
 TEST(SmallSet, MoveConstructor) {
@@ -42,7 +45,7 @@ TEST(SmallSet, MoveConstructor) {
   SmallSet<int, 4> S = L;
   SmallSet<int, 4> T = std::move(S);
 
-  EXPECT_THAT(T, testing::UnorderedElementsAreArray(L));
+  CHECK_THAT(T, Catch::Matchers::UnorderedRangeEquals(L));
 }
 
 TEST(SmallSet, CopyAssignment) {
@@ -50,7 +53,7 @@ TEST(SmallSet, CopyAssignment) {
   SmallSet<int, 4> T;
   T = S;
 
-  EXPECT_THAT(S, testing::ContainerEq(T));
+  CHECK_THAT(S, Catch::Matchers::RangeEquals(T));
 }
 
 TEST(SmallSet, MoveAssignment) {
@@ -59,7 +62,7 @@ TEST(SmallSet, MoveAssignment) {
   SmallSet<int, 4> T;
   T = std::move(S);
 
-  EXPECT_THAT(T, testing::UnorderedElementsAreArray(L));
+  CHECK_THAT(T, Catch::Matchers::UnorderedRangeEquals(L));
 }
 
 TEST(SmallSetTest, Insert) {
@@ -123,14 +126,14 @@ TEST(SmallSetTest, InsertPerfectFwd) {
 TEST(SmallSetTest, CtorRange) {
   constexpr unsigned Args[] = {3, 1, 2};
   SmallSet<int, 4> s1(llvm::from_range, Args);
-  EXPECT_THAT(s1, ::testing::UnorderedElementsAre(1, 2, 3));
+  CHECK_THAT(s1, Catch::Matchers::UnorderedRangeEquals({1, 2, 3}));
 }
 
 TEST(SmallSetTest, InsertRange) {
   SmallSet<int, 4> s1;
   constexpr unsigned Args[] = {3, 1, 2};
   s1.insert_range(Args);
-  EXPECT_THAT(s1, ::testing::UnorderedElementsAre(1, 2, 3));
+  CHECK_THAT(s1, Catch::Matchers::UnorderedRangeEquals({1, 2, 3}));
 }
 
 TEST(SmallSetTest, Grow) {
diff --git a/llvm/unittests/ADT/SmallStringTest.cpp b/llvm/unittests/ADT/SmallStringTest.cpp
index e8c8085f4620628764d866c5fd042777de92d820..c8ed241ec48c0a7d900a985d5ef7b673acc28bfa 100644
--- a/llvm/unittests/ADT/SmallStringTest.cpp
+++ b/llvm/unittests/ADT/SmallStringTest.cpp
@@ -11,17 +11,22 @@
 //===----------------------------------------------------------------------===//
 
 #include "llvm/ADT/SmallString.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 #include <climits>
 #include <cstring>
 #include <stdarg.h>
+#include <string_view>
 
 using namespace llvm;
 
 namespace {
 
 // Test fixture class
-class SmallStringTest : public testing::Test {
+class SmallStringTest {
 protected:
   using StringType = SmallString<40>;
 
@@ -32,7 +37,7 @@ protected:
     EXPECT_EQ(0u, v.size());
     EXPECT_TRUE(v.empty());
     // Iterator tests
-    EXPECT_TRUE(v.begin() == v.end());
+    EXPECT_EQ(v.begin(), v.end());
   }
 };
 
@@ -40,27 +45,27 @@ protected:
 TEST_F(SmallStringTest, EmptyStringTest) {
   SCOPED_TRACE("EmptyStringTest");
   assertEmpty(theString);
-  EXPECT_TRUE(theString.rbegin() == theString.rend());
+  EXPECT_EQ(theString.rbegin(), theString.rend());
 }
 
 TEST_F(SmallStringTest, AssignRepeated) {
   theString.assign(3, 'a');
   EXPECT_EQ(3u, theString.size());
-  EXPECT_STREQ("aaa", theString.c_str());
+  EXPECT_EQ(std::string_view{"aaa"}, std::string_view{theString.c_str()});
 }
 
 TEST_F(SmallStringTest, AssignIterPair) {
   std::string_view abc = "abc";
   theString.assign(abc.begin(), abc.end());
   EXPECT_EQ(3u, theString.size());
-  EXPECT_STREQ("abc", theString.c_str());
+  EXPECT_EQ(std::string_view{"abc"}, std::string_view{theString.c_str()});
 }
 
 TEST_F(SmallStringTest, AssignStringView) {
   std::string_view abc = "abc";
   theString.assign(abc);
   EXPECT_EQ(3u, theString.size());
-  EXPECT_STREQ("abc", theString.c_str());
+  EXPECT_EQ(std::string_view{"abc"}, std::string_view{theString.c_str()});
 }
 
 TEST_F(SmallStringTest, AssignSmallVector) {
@@ -68,13 +73,13 @@ TEST_F(SmallStringTest, AssignSmallVector) {
   SmallVector<char, 10> abcVec(abc.begin(), abc.end());
   theString.assign(abcVec);
   EXPECT_EQ(3u, theString.size());
-  EXPECT_STREQ("abc", theString.c_str());
+  EXPECT_EQ(std::string_view{"abc"}, std::string_view{theString.c_str()});
 }
 
 TEST_F(SmallStringTest, AssignStringViews) {
   theString.assign({"abc", "def", "ghi"});
   EXPECT_EQ(9u, theString.size());
-  EXPECT_STREQ("abcdefghi", theString.c_str());
+  EXPECT_EQ(std::string_view{"abcdefghi"}, std::string_view{theString.c_str()});
 }
 
 TEST_F(SmallStringTest, AppendIterPair) {
@@ -82,7 +87,7 @@ TEST_F(SmallStringTest, AppendIterPair) {
   theString.append(abc.begin(), abc.end());
   theString.append(abc.begin(), abc.end());
   EXPECT_EQ(6u, theString.size());
-  EXPECT_STREQ("abcabc", theString.c_str());
+  EXPECT_EQ(std::string_view{"abcabc"}, std::string_view{theString.c_str()});
 }
 
 TEST_F(SmallStringTest, AppendStringView) {
@@ -90,7 +95,7 @@ TEST_F(SmallStringTest, AppendStringView) {
   theString.append(abc);
   theString.append(abc);
   EXPECT_EQ(6u, theString.size());
-  EXPECT_STREQ("abcabc", theString.c_str());
+  EXPECT_EQ(std::string_view{"abcabc"}, std::string_view{theString.c_str()});
 }
 
 TEST_F(SmallStringTest, AppendSmallVector) {
@@ -99,20 +104,20 @@ TEST_F(SmallStringTest, AppendSmallVector) {
   theString.append(abcVec);
   theString.append(abcVec);
   EXPECT_EQ(6u, theString.size());
-  EXPECT_STREQ("abcabc", theString.c_str());
+  EXPECT_EQ(std::string_view{"abcabc"}, std::string_view{theString.c_str()});
 }
 
 TEST_F(SmallStringTest, AppendStringViews) {
   theString.append({"abc", "def", "ghi"});
   EXPECT_EQ(9u, theString.size());
-  EXPECT_STREQ("abcdefghi", theString.c_str());
+  EXPECT_EQ(std::string_view{"abcdefghi"}, std::string_view{theString.c_str()});
   std::string_view Jkl = "jkl";
   std::string Mno = "mno";
   SmallString<4> Pqr("pqr");
   const char *Stu = "stu";
   theString.append({Jkl, Mno, Pqr, Stu});
   EXPECT_EQ(21u, theString.size());
-  EXPECT_STREQ("abcdefghijklmnopqrstu", theString.c_str());
+  EXPECT_EQ(std::string_view{"abcdefghijklmnopqrstu"}, std::string_view{theString.c_str()});
 }
 
 TEST_F(SmallStringTest, StringViewConversion) {
diff --git a/llvm/unittests/ADT/SmallVectorTest.cpp b/llvm/unittests/ADT/SmallVectorTest.cpp
index f7a3110f183820212179644fd31009685e172943..ac88d582d0ca8468d26f6a37af8a609617bcc607 100644
--- a/llvm/unittests/ADT/SmallVectorTest.cpp
+++ b/llvm/unittests/ADT/SmallVectorTest.cpp
@@ -12,12 +12,24 @@
 
 #include "llvm/ADT/SmallVector.h"
 #include "llvm/Support/Compiler.h"
-#include "gmock/gmock.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 #include <array>
+#include <csignal>
+#include <cstdlib>
 #include <list>
+#include <regex>
 #include <span>
 #include <stdarg.h>
+#include <string>
+
+#ifndef _WIN32
+#include <sys/wait.h>
+#include <unistd.h>
+#endif
 
 #if defined(__GNUC__)
 #pragma GCC diagnostic ignored "-Wpedantic"
@@ -27,6 +39,61 @@ using namespace llvm;
 
 namespace {
 
+#ifdef _WIN32
+#define WPIUTIL_HAS_DEATH_TEST 0
+#define CHECK_DEATH(statement, matcher) \
+  SKIP("death tests are not supported by Catch2 on Windows")
+#else
+#define WPIUTIL_HAS_DEATH_TEST 1
+template <typename F>
+bool CheckDeathImpl(F&& func, std::string_view matcher) {
+  int pipeFds[2];
+  if (pipe(pipeFds) != 0) {
+    return false;
+  }
+
+  pid_t pid = fork();
+  if (pid == 0) {
+    std::signal(SIGABRT, SIG_DFL);
+    std::signal(SIGBUS, SIG_DFL);
+    std::signal(SIGFPE, SIG_DFL);
+    std::signal(SIGILL, SIG_DFL);
+    std::signal(SIGSEGV, SIG_DFL);
+
+    close(pipeFds[0]);
+    dup2(pipeFds[1], STDERR_FILENO);
+    close(pipeFds[1]);
+    func();
+    std::_Exit(0);
+  }
+
+  close(pipeFds[1]);
+  std::string output;
+  char buffer[512];
+  ssize_t count;
+  while ((count = read(pipeFds[0], buffer, sizeof(buffer))) > 0) {
+    output.append(buffer, count);
+  }
+  close(pipeFds[0]);
+
+  int status = 0;
+  if (waitpid(pid, &status, 0) < 0) {
+    return false;
+  }
+
+  bool died = WIFSIGNALED(status) || (WIFEXITED(status) && WEXITSTATUS(status));
+  bool outputMatches =
+      std::regex_search(output, std::regex{std::string{matcher}});
+  if (!died || !outputMatches) {
+    SCOPED_TRACE("Death test output: " << output);
+  }
+  return died && outputMatches;
+}
+
+#define CHECK_DEATH(statement, matcher) \
+  EXPECT_TRUE(CheckDeathImpl([&] { statement; }, matcher))
+#endif
+
 /// A helper class that counts the total number of constructor and
 /// destructor calls.
 class Constructable {
@@ -203,7 +270,7 @@ template <typename VectorT> void assertEmpty(VectorT &v) {
   EXPECT_TRUE(v.empty());
 
   // Iterator tests
-  EXPECT_TRUE(v.begin() == v.end());
+  EXPECT_EQ(v.begin(), v.end());
 }
 
 // Generate a sequence of values to initialize the vector.
@@ -218,9 +285,9 @@ constexpr static unsigned NumBuiltinElts(const SmallVector<T, N> &) {
   return N;
 }
 
-class SmallVectorTestBase : public testing::Test {
+class SmallVectorTestBase {
 protected:
-  void SetUp() override { Constructable::reset(); }
+  SmallVectorTestBase() { Constructable::reset(); }
 };
 
 // Test fixture class
@@ -231,14 +298,13 @@ protected:
   VectorT otherVector;
 };
 
-using SmallVectorTestTypes = ::testing::Types<
-    SmallVector<Constructable, 0>, SmallVector<Constructable, 1>,
-    SmallVector<Constructable, 2>, SmallVector<Constructable, 4>,
-    SmallVector<Constructable, 5>>;
-TYPED_TEST_SUITE(SmallVectorTest, SmallVectorTestTypes, );
+#define WPIUTIL_TEST_TYPES_SmallVectorTest \
+  (SmallVector<Constructable, 0>), (SmallVector<Constructable, 1>), \
+      (SmallVector<Constructable, 2>), (SmallVector<Constructable, 4>), \
+      (SmallVector<Constructable, 5>)
 
 // Constructor test.
-TYPED_TEST(SmallVectorTest, ConstructorNonIterTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest ConstructorNonIterTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("ConstructorTest");
   auto &V = this->theVector;
   V = SmallVector<Constructable, 2>(2, 2);
@@ -246,7 +312,7 @@ TYPED_TEST(SmallVectorTest, ConstructorNonIterTest) {
 }
 
 // Constructor test.
-TYPED_TEST(SmallVectorTest, ConstructorIterTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest ConstructorIterTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("ConstructorTest");
   int arr[] = {1, 2, 3};
   auto &V = this->theVector;
@@ -255,7 +321,7 @@ TYPED_TEST(SmallVectorTest, ConstructorIterTest) {
 }
 
 // Constructor test.
-TYPED_TEST(SmallVectorTest, ConstructorFromSpanSimpleTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest ConstructorFromSpanSimpleTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("ConstructorFromSpanSimpleTest");
   std::array<Constructable, 3> StdArray = {Constructable(1), Constructable(2),
                                            Constructable(3)};
@@ -263,21 +329,21 @@ TYPED_TEST(SmallVectorTest, ConstructorFromSpanSimpleTest) {
   auto &V = this->theVector;
   V = SmallVector<Constructable, 4>(Array);
   assertValuesInOrder(V, 3u, 1, 2, 3);
-  ASSERT_EQ(NumBuiltinElts(TypeParam{}), NumBuiltinElts(V));
+  ASSERT_EQ(NumBuiltinElts(TestType{}), NumBuiltinElts(V));
 }
 
 // New vector test.
-TYPED_TEST(SmallVectorTest, EmptyVectorTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest EmptyVectorTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("EmptyVectorTest");
   auto &V = this->theVector;
   assertEmpty(V);
-  EXPECT_TRUE(V.rbegin() == V.rend());
+  EXPECT_EQ(V.rbegin(), V.rend());
   EXPECT_EQ(0, Constructable::getNumConstructorCalls());
   EXPECT_EQ(0, Constructable::getNumDestructorCalls());
 }
 
 // Simple insertions and deletions.
-TYPED_TEST(SmallVectorTest, PushPopTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest PushPopTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("PushPopTest");
   auto &V = this->theVector;
   // Track whether the vector will potentially have to grow.
@@ -319,13 +385,12 @@ TYPED_TEST(SmallVectorTest, PushPopTest) {
     // If we had to grow the vector, these only have a lower bound, but should
     // always be equal.
     EXPECT_LE(5, Constructable::getNumConstructorCalls());
-    EXPECT_EQ(Constructable::getNumConstructorCalls(),
-              Constructable::getNumDestructorCalls());
+    EXPECT_EQ(Constructable::getNumConstructorCalls(), Constructable::getNumDestructorCalls());
   }
 }
 
 // Clear test.
-TYPED_TEST(SmallVectorTest, ClearTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest ClearTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("ClearTest");
   auto &V = this->theVector;
   V.reserve(2);
@@ -338,7 +403,7 @@ TYPED_TEST(SmallVectorTest, ClearTest) {
 }
 
 // Resize smaller test.
-TYPED_TEST(SmallVectorTest, ResizeShrinkTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest ResizeShrinkTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("ResizeShrinkTest");
   auto &V = this->theVector;
   V.reserve(3);
@@ -351,7 +416,7 @@ TYPED_TEST(SmallVectorTest, ResizeShrinkTest) {
 }
 
 // Truncate test.
-TYPED_TEST(SmallVectorTest, TruncateTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest TruncateTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("TruncateTest");
   auto &V = this->theVector;
   V.reserve(3);
@@ -362,8 +427,8 @@ TYPED_TEST(SmallVectorTest, TruncateTest) {
   EXPECT_EQ(6, Constructable::getNumConstructorCalls());
   EXPECT_EQ(5, Constructable::getNumDestructorCalls());
 
-#if !defined(NDEBUG) && GTEST_HAS_DEATH_TEST
-  EXPECT_DEATH(V.truncate(2), "Cannot increase size");
+#if !defined(NDEBUG) && WPIUTIL_HAS_DEATH_TEST
+  CHECK_DEATH(V.truncate(2), "Cannot increase size");
 #endif
   V.truncate(1);
   assertValuesInOrder(V, 1u, 1);
@@ -377,7 +442,7 @@ TYPED_TEST(SmallVectorTest, TruncateTest) {
 }
 
 // Resize bigger test.
-TYPED_TEST(SmallVectorTest, ResizeGrowTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest ResizeGrowTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("ResizeGrowTest");
   auto &V = this->theVector;
   V.resize(2);
@@ -387,7 +452,7 @@ TYPED_TEST(SmallVectorTest, ResizeGrowTest) {
   EXPECT_EQ(2u, V.size());
 }
 
-TYPED_TEST(SmallVectorTest, ResizeWithElementsTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest ResizeWithElementsTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   auto &V = this->theVector;
   V.resize(2);
 
@@ -396,15 +461,15 @@ TYPED_TEST(SmallVectorTest, ResizeWithElementsTest) {
   V.resize(4);
 
   size_t Ctors = Constructable::getNumConstructorCalls();
-  EXPECT_TRUE(Ctors == 2 || Ctors == 4);
+  EXPECT_TRUE((Ctors == 2 || Ctors == 4));
   size_t MoveCtors = Constructable::getNumMoveConstructorCalls();
-  EXPECT_TRUE(MoveCtors == 0 || MoveCtors == 2);
+  EXPECT_TRUE((MoveCtors == 0 || MoveCtors == 2));
   size_t Dtors = Constructable::getNumDestructorCalls();
-  EXPECT_TRUE(Dtors == 0 || Dtors == 2);
+  EXPECT_TRUE((Dtors == 0 || Dtors == 2));
 }
 
 // Resize with fill value.
-TYPED_TEST(SmallVectorTest, ResizeFillTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest ResizeFillTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("ResizeFillTest");
   auto &V = this->theVector;
   V.resize(3, Constructable(77));
@@ -437,7 +502,7 @@ TEST(SmallVectorTest, ResizeForOverwrite) {
 }
 
 // Overflow past fixed size.
-TYPED_TEST(SmallVectorTest, OverflowTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest OverflowTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("OverflowTest");
   auto &V = this->theVector;
   // Push more elements than the fixed size.
@@ -456,47 +521,47 @@ TYPED_TEST(SmallVectorTest, OverflowTest) {
 }
 
 // Iteration tests.
-TYPED_TEST(SmallVectorTest, IterationTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest IterationTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   auto &V = this->theVector;
   makeSequence(V, 1, 2);
 
   // Forward Iteration
-  typename TypeParam::iterator it = V.begin();
-  EXPECT_TRUE(*it == V.front());
-  EXPECT_TRUE(*it == V[0]);
+  typename TestType::iterator it = V.begin();
+  EXPECT_EQ(*it, V.front());
+  EXPECT_EQ(*it, V[0]);
   EXPECT_EQ(1, it->getValue());
   ++it;
-  EXPECT_TRUE(*it == V[1]);
-  EXPECT_TRUE(*it == V.back());
+  EXPECT_EQ(*it, V[1]);
+  EXPECT_EQ(*it, V.back());
   EXPECT_EQ(2, it->getValue());
   ++it;
-  EXPECT_TRUE(it == V.end());
+  EXPECT_EQ(it, V.end());
   --it;
-  EXPECT_TRUE(*it == V[1]);
+  EXPECT_EQ(*it, V[1]);
   EXPECT_EQ(2, it->getValue());
   --it;
-  EXPECT_TRUE(*it == V[0]);
+  EXPECT_EQ(*it, V[0]);
   EXPECT_EQ(1, it->getValue());
 
   // Reverse Iteration
-  typename TypeParam::reverse_iterator rit = V.rbegin();
-  EXPECT_TRUE(*rit == V[1]);
+  typename TestType::reverse_iterator rit = V.rbegin();
+  EXPECT_EQ(*rit, V[1]);
   EXPECT_EQ(2, rit->getValue());
   ++rit;
-  EXPECT_TRUE(*rit == V[0]);
+  EXPECT_EQ(*rit, V[0]);
   EXPECT_EQ(1, rit->getValue());
   ++rit;
-  EXPECT_TRUE(rit == V.rend());
+  EXPECT_EQ(rit, V.rend());
   --rit;
-  EXPECT_TRUE(*rit == V[0]);
+  EXPECT_EQ(*rit, V[0]);
   EXPECT_EQ(1, rit->getValue());
   --rit;
-  EXPECT_TRUE(*rit == V[1]);
+  EXPECT_EQ(*rit, V[1]);
   EXPECT_EQ(2, rit->getValue());
 }
 
 // Swap test.
-TYPED_TEST(SmallVectorTest, SwapTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest SwapTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("SwapTest");
   auto &V = this->theVector;
   auto &U = this->otherVector;
@@ -508,7 +573,7 @@ TYPED_TEST(SmallVectorTest, SwapTest) {
 }
 
 // Append test
-TYPED_TEST(SmallVectorTest, AppendTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest AppendTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("AppendTest");
   auto &V = this->theVector;
   auto &U = this->otherVector;
@@ -521,7 +586,7 @@ TYPED_TEST(SmallVectorTest, AppendTest) {
 }
 
 // Append repeated test
-TYPED_TEST(SmallVectorTest, AppendRepeatedTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest AppendRepeatedTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("AppendRepeatedTest");
   auto &V = this->theVector;
   V.push_back(Constructable(1));
@@ -530,7 +595,7 @@ TYPED_TEST(SmallVectorTest, AppendRepeatedTest) {
 }
 
 // Append test
-TYPED_TEST(SmallVectorTest, AppendNonIterTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest AppendNonIterTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("AppendRepeatedTest");
   auto &V = this->theVector;
   V.push_back(Constructable(1));
@@ -548,7 +613,7 @@ struct output_iterator {
   operator Constructable() { return 7; }
 };
 
-TYPED_TEST(SmallVectorTest, AppendRepeatedNonForwardIterator) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest AppendRepeatedNonForwardIterator", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("AppendRepeatedTest");
   auto &V = this->theVector;
   V.push_back(Constructable(1));
@@ -556,7 +621,7 @@ TYPED_TEST(SmallVectorTest, AppendRepeatedNonForwardIterator) {
   assertValuesInOrder(V, 3u, 1, 7, 7);
 }
 
-TYPED_TEST(SmallVectorTest, AppendSmallVector) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest AppendSmallVector", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("AppendSmallVector");
   auto &V = this->theVector;
   SmallVector<Constructable, 3> otherVector = {7, 7};
@@ -566,7 +631,7 @@ TYPED_TEST(SmallVectorTest, AppendSmallVector) {
 }
 
 // Assign test
-TYPED_TEST(SmallVectorTest, AssignTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest AssignTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("AssignTest");
   auto &V = this->theVector;
   V.push_back(Constructable(1));
@@ -575,7 +640,7 @@ TYPED_TEST(SmallVectorTest, AssignTest) {
 }
 
 // Assign test
-TYPED_TEST(SmallVectorTest, AssignRangeTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest AssignRangeTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("AssignTest");
   auto &V = this->theVector;
   V.push_back(Constructable(1));
@@ -585,7 +650,7 @@ TYPED_TEST(SmallVectorTest, AssignRangeTest) {
 }
 
 // Assign test
-TYPED_TEST(SmallVectorTest, AssignNonIterTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest AssignNonIterTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("AssignTest");
   auto &V = this->theVector;
   V.push_back(Constructable(1));
@@ -593,7 +658,7 @@ TYPED_TEST(SmallVectorTest, AssignNonIterTest) {
   assertValuesInOrder(V, 2u, 7, 7);
 }
 
-TYPED_TEST(SmallVectorTest, AssignSmallVector) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest AssignSmallVector", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("AssignSmallVector");
   auto &V = this->theVector;
   SmallVector<Constructable, 3> otherVector = {7, 7};
@@ -602,7 +667,7 @@ TYPED_TEST(SmallVectorTest, AssignSmallVector) {
   assertValuesInOrder(V, 2u, 7, 7);
 }
 
-TYPED_TEST(SmallVectorTest, AssignSpan) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest AssignSpan", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("AssignSpan");
   auto &V = this->theVector;
   Constructable Other[] = {7, 8, 9};
@@ -612,7 +677,7 @@ TYPED_TEST(SmallVectorTest, AssignSpan) {
 }
 
 // Move-assign test
-TYPED_TEST(SmallVectorTest, MoveAssignTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest MoveAssignTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("MoveAssignTest");
   auto &V = this->theVector;
   auto &U = this->otherVector;
@@ -633,17 +698,15 @@ TYPED_TEST(SmallVectorTest, MoveAssignTest) {
   // Make sure the # of constructor/destructor calls line up. There
   // are two live objects after clearing the other vector.
   U.clear();
-  EXPECT_EQ(Constructable::getNumConstructorCalls()-2, 
-            Constructable::getNumDestructorCalls());
+  EXPECT_EQ(Constructable::getNumConstructorCalls()-2, Constructable::getNumDestructorCalls());
 
   // There shouldn't be any live objects any more.
   V.clear();
-  EXPECT_EQ(Constructable::getNumConstructorCalls(), 
-            Constructable::getNumDestructorCalls());
+  EXPECT_EQ(Constructable::getNumConstructorCalls(), Constructable::getNumDestructorCalls());
 }
 
 // Erase a single element
-TYPED_TEST(SmallVectorTest, EraseTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest EraseTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("EraseTest");
   auto &V = this->theVector;
   makeSequence(V, 1, 3);
@@ -653,7 +716,7 @@ TYPED_TEST(SmallVectorTest, EraseTest) {
 }
 
 // Erase a range of elements
-TYPED_TEST(SmallVectorTest, EraseRangeTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest EraseRangeTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("EraseRangeTest");
   auto &V = this->theVector;
   makeSequence(V, 1, 3);
@@ -663,28 +726,28 @@ TYPED_TEST(SmallVectorTest, EraseRangeTest) {
 }
 
 // Insert a single element.
-TYPED_TEST(SmallVectorTest, InsertTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest InsertTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("InsertTest");
   auto &V = this->theVector;
   makeSequence(V, 1, 3);
-  typename TypeParam::iterator I = V.insert(V.begin() + 1, Constructable(77));
+  typename TestType::iterator I = V.insert(V.begin() + 1, Constructable(77));
   EXPECT_EQ(V.begin() + 1, I);
   assertValuesInOrder(V, 4u, 1, 77, 2, 3);
 }
 
 // Insert a copy of a single element.
-TYPED_TEST(SmallVectorTest, InsertCopy) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest InsertCopy", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("InsertTest");
   auto &V = this->theVector;
   makeSequence(V, 1, 3);
   Constructable C(77);
-  typename TypeParam::iterator I = V.insert(V.begin() + 1, C);
+  typename TestType::iterator I = V.insert(V.begin() + 1, C);
   EXPECT_EQ(V.begin() + 1, I);
   assertValuesInOrder(V, 4u, 1, 77, 2, 3);
 }
 
 // Insert repeated elements.
-TYPED_TEST(SmallVectorTest, InsertRepeatedTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest InsertRepeatedTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("InsertRepeatedTest");
   auto &V = this->theVector;
   makeSequence(V, 1, 4);
@@ -695,8 +758,8 @@ TYPED_TEST(SmallVectorTest, InsertRepeatedTest) {
   // FIXME: This is inefficient, we shouldn't move things into newly allocated
   // space, then move them up/around, there should only be 2 or 4 move
   // constructions here.
-  EXPECT_TRUE(Constructable::getNumMoveConstructorCalls() == 2 ||
-              Constructable::getNumMoveConstructorCalls() == 6);
+  EXPECT_TRUE((Constructable::getNumMoveConstructorCalls() == 2 ||
+         Constructable::getNumMoveConstructorCalls() == 6));
   // Move assign the next two to shift them up and make a gap.
   EXPECT_EQ(1, Constructable::getNumMoveAssignmentCalls());
   // Copy construct the two new elements from the parameter.
@@ -707,7 +770,7 @@ TYPED_TEST(SmallVectorTest, InsertRepeatedTest) {
   assertValuesInOrder(V, 6u, 1, 16, 16, 2, 3, 4);
 }
 
-TYPED_TEST(SmallVectorTest, InsertRepeatedNonIterTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest InsertRepeatedNonIterTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("InsertRepeatedTest");
   auto &V = this->theVector;
   makeSequence(V, 1, 4);
@@ -717,7 +780,7 @@ TYPED_TEST(SmallVectorTest, InsertRepeatedNonIterTest) {
   assertValuesInOrder(V, 6u, 1, 7, 7, 2, 3, 4);
 }
 
-TYPED_TEST(SmallVectorTest, InsertRepeatedAtEndTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest InsertRepeatedAtEndTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("InsertRepeatedTest");
   auto &V = this->theVector;
   makeSequence(V, 1, 4);
@@ -726,8 +789,8 @@ TYPED_TEST(SmallVectorTest, InsertRepeatedAtEndTest) {
   // Just copy construct them into newly allocated space
   EXPECT_EQ(2, Constructable::getNumCopyConstructorCalls());
   // Move everything across if reallocation is needed.
-  EXPECT_TRUE(Constructable::getNumMoveConstructorCalls() == 0 ||
-              Constructable::getNumMoveConstructorCalls() == 4);
+  EXPECT_TRUE((Constructable::getNumMoveConstructorCalls() == 0 ||
+         Constructable::getNumMoveConstructorCalls() == 4));
   // Without ever moving or copying anything else.
   EXPECT_EQ(0, Constructable::getNumCopyAssignmentCalls());
   EXPECT_EQ(0, Constructable::getNumMoveAssignmentCalls());
@@ -736,7 +799,7 @@ TYPED_TEST(SmallVectorTest, InsertRepeatedAtEndTest) {
   assertValuesInOrder(V, 6u, 1, 2, 3, 4, 16, 16);
 }
 
-TYPED_TEST(SmallVectorTest, InsertRepeatedEmptyTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest InsertRepeatedEmptyTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("InsertRepeatedTest");
   auto &V = this->theVector;
   makeSequence(V, 10, 15);
@@ -747,7 +810,7 @@ TYPED_TEST(SmallVectorTest, InsertRepeatedEmptyTest) {
 }
 
 // Insert range.
-TYPED_TEST(SmallVectorTest, InsertRangeTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest InsertRangeTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("InsertRangeTest");
   auto &V = this->theVector;
   Constructable Arr[3] =
@@ -761,8 +824,8 @@ TYPED_TEST(SmallVectorTest, InsertRangeTest) {
   // FIXME: This is inefficient, we shouldn't move things into newly allocated
   // space, then move them up/around, there should only be 2 or 3 move
   // constructions here.
-  EXPECT_TRUE(Constructable::getNumMoveConstructorCalls() == 2 ||
-              Constructable::getNumMoveConstructorCalls() == 5);
+  EXPECT_TRUE((Constructable::getNumMoveConstructorCalls() == 2 ||
+         Constructable::getNumMoveConstructorCalls() == 5));
   // Copy assign the lower 2 new elements into existing space.
   EXPECT_EQ(2, Constructable::getNumCopyAssignmentCalls());
   // Copy construct the third element into newly allocated space.
@@ -772,7 +835,7 @@ TYPED_TEST(SmallVectorTest, InsertRangeTest) {
 }
 
 
-TYPED_TEST(SmallVectorTest, InsertRangeAtEndTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest InsertRangeAtEndTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("InsertRangeTest");
   auto &V = this->theVector;
   Constructable Arr[3] =
@@ -789,14 +852,14 @@ TYPED_TEST(SmallVectorTest, InsertRangeAtEndTest) {
   EXPECT_EQ(0, Constructable::getNumCopyAssignmentCalls());
   // Reallocation might occur, causing all elements to be moved into the new
   // buffer.
-  EXPECT_TRUE(Constructable::getNumMoveConstructorCalls() == 0 ||
-              Constructable::getNumMoveConstructorCalls() == 3);
+  EXPECT_TRUE((Constructable::getNumMoveConstructorCalls() == 0 ||
+         Constructable::getNumMoveConstructorCalls() == 3));
   EXPECT_EQ(0, Constructable::getNumMoveAssignmentCalls());
   EXPECT_EQ(V.begin() + 3, I);
   assertValuesInOrder(V, 6u, 1, 2, 3, 77, 77, 77);
 }
 
-TYPED_TEST(SmallVectorTest, InsertEmptyRangeTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest InsertEmptyRangeTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("InsertRangeTest");
   auto &V = this->theVector;
   makeSequence(V, 1, 3);
@@ -807,65 +870,65 @@ TYPED_TEST(SmallVectorTest, InsertEmptyRangeTest) {
 }
 
 // Comparison tests.
-TYPED_TEST(SmallVectorTest, ComparisonEqualityTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest ComparisonEqualityTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("ComparisonEqualityTest");
   auto &V = this->theVector;
   auto &U = this->otherVector;
   makeSequence(V, 1, 3);
   makeSequence(U, 1, 3);
 
-  EXPECT_TRUE(V == U);
+  EXPECT_EQ(V, U);
   EXPECT_FALSE(V != U);
 
   U.clear();
   makeSequence(U, 2, 4);
 
   EXPECT_FALSE(V == U);
-  EXPECT_TRUE(V != U);
+  EXPECT_NE(V, U);
 }
 
 // Comparison tests.
-TYPED_TEST(SmallVectorTest, ComparisonLessThanTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest ComparisonLessThanTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   SCOPED_TRACE("ComparisonLessThanTest");
   auto &V = this->theVector;
   auto &U = this->otherVector;
   V = {1, 2, 4};
   U = {1, 4};
 
-  EXPECT_TRUE(V < U);
-  EXPECT_TRUE(V <= U);
+  EXPECT_LT(V, U);
+  EXPECT_LE(V, U);
   EXPECT_FALSE(V > U);
   EXPECT_FALSE(V >= U);
 
   EXPECT_FALSE(U < V);
   EXPECT_FALSE(U <= V);
-  EXPECT_TRUE(U > V);
-  EXPECT_TRUE(U >= V);
+  EXPECT_GT(U, V);
+  EXPECT_GE(U, V);
 
   U = {1, 2, 4};
 
   EXPECT_FALSE(V < U);
-  EXPECT_TRUE(V <= U);
+  EXPECT_LE(V, U);
   EXPECT_FALSE(V > U);
-  EXPECT_TRUE(V >= U);
+  EXPECT_GE(V, U);
 
   EXPECT_FALSE(U < V);
-  EXPECT_TRUE(U <= V);
+  EXPECT_LE(U, V);
   EXPECT_FALSE(U > V);
-  EXPECT_TRUE(U >= V);
+  EXPECT_GE(U, V);
 }
 
 // Constant vector tests.
-TYPED_TEST(SmallVectorTest, ConstVectorTest) {
-  const TypeParam constVector;
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest ConstVectorTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
+  const TestType constVector;
 
   EXPECT_EQ(0u, constVector.size());
   EXPECT_TRUE(constVector.empty());
-  EXPECT_TRUE(constVector.begin() == constVector.end());
+  EXPECT_EQ(constVector.begin(), constVector.end());
 }
 
 // Direct array access.
-TYPED_TEST(SmallVectorTest, DirectVectorTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest DirectVectorTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   auto &V = this->theVector;
   EXPECT_EQ(0u, V.size());
   V.reserve(4);
@@ -883,7 +946,7 @@ TYPED_TEST(SmallVectorTest, DirectVectorTest) {
   EXPECT_EQ(4, V[3].getValue());
 }
 
-TYPED_TEST(SmallVectorTest, IteratorTest) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorTest, "SmallVectorTest IteratorTest", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorTest) {
   auto &V = this->theVector;
   std::list<int> L;
   V.insert(V.end(), L.begin(), L.end());
@@ -898,19 +961,13 @@ protected:
   VectorT2 otherVector;
 };
 
-using DualSmallVectorTestTypes = ::testing::Types<
-    // Small mode -> Small mode.
-    std::pair<SmallVector<Constructable, 4>, SmallVector<Constructable, 4>>,
-    // Small mode -> Big mode.
-    std::pair<SmallVector<Constructable, 4>, SmallVector<Constructable, 2>>,
-    // Big mode -> Small mode.
-    std::pair<SmallVector<Constructable, 2>, SmallVector<Constructable, 4>>,
-    // Big mode -> Big mode.
-    std::pair<SmallVector<Constructable, 2>, SmallVector<Constructable, 2>>>;
+#define WPIUTIL_TEST_TYPES_DualSmallVectorsTest \
+  (std::pair<SmallVector<Constructable, 4>, SmallVector<Constructable, 4>>), \
+      (std::pair<SmallVector<Constructable, 4>, SmallVector<Constructable, 2>>), \
+      (std::pair<SmallVector<Constructable, 2>, SmallVector<Constructable, 4>>), \
+      (std::pair<SmallVector<Constructable, 2>, SmallVector<Constructable, 2>>)
 
-TYPED_TEST_SUITE(DualSmallVectorsTest, DualSmallVectorTestTypes, );
-
-TYPED_TEST(DualSmallVectorsTest, MoveAssignment) {
+TEMPLATE_TEST_CASE_METHOD(DualSmallVectorsTest, "DualSmallVectorsTest MoveAssignment", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_DualSmallVectorsTest) {
   SCOPED_TRACE("MoveAssignTest-DualVectorTypes");
   auto &V = this->theVector;
   auto &U = this->otherVector;
@@ -929,17 +986,15 @@ TYPED_TEST(DualSmallVectorsTest, MoveAssignment) {
   // Make sure the # of constructor/destructor calls line up. There
   // are two live objects after clearing the other vector.
   U.clear();
-  EXPECT_EQ(Constructable::getNumConstructorCalls()-4,
-            Constructable::getNumDestructorCalls());
+  EXPECT_EQ(Constructable::getNumConstructorCalls()-4, Constructable::getNumDestructorCalls());
 
   // If the source vector (otherVector) was in small-mode, assert that we just
   // moved the data pointer over.
-  EXPECT_TRUE(NumBuiltinElts(U) == 4 || V.data() == OrigDataPtr);
+  EXPECT_TRUE((NumBuiltinElts(U) == 4 || V.data() == OrigDataPtr));
 
   // There shouldn't be any live objects any more.
   V.clear();
-  EXPECT_EQ(Constructable::getNumConstructorCalls(),
-            Constructable::getNumDestructorCalls());
+  EXPECT_EQ(Constructable::getNumConstructorCalls(), Constructable::getNumDestructorCalls());
 
   // We shouldn't have copied anything in this whole process.
   EXPECT_EQ(Constructable::getNumCopyConstructorCalls(), 0);
@@ -1051,68 +1106,68 @@ TEST(SmallVectorTest, EmplaceBack) {
   {
     SmallVector<Emplaceable, 3> V;
     Emplaceable &back = V.emplace_back();
-    EXPECT_TRUE(&back == &V.back());
-    EXPECT_TRUE(V.size() == 1);
-    EXPECT_TRUE(back.State == ES_Emplaced);
-    EXPECT_TRUE(back.A0.State == EAS_Defaulted);
-    EXPECT_TRUE(back.A1.State == EAS_Defaulted);
-    EXPECT_TRUE(back.A2.State == EAS_Defaulted);
-    EXPECT_TRUE(back.A3.State == EAS_Defaulted);
+    EXPECT_EQ(&back, &V.back());
+    EXPECT_EQ(V.size(), 1);
+    EXPECT_EQ(back.State, ES_Emplaced);
+    EXPECT_EQ(back.A0.State, EAS_Defaulted);
+    EXPECT_EQ(back.A1.State, EAS_Defaulted);
+    EXPECT_EQ(back.A2.State, EAS_Defaulted);
+    EXPECT_EQ(back.A3.State, EAS_Defaulted);
   }
   {
     SmallVector<Emplaceable, 3> V;
     Emplaceable &back = V.emplace_back(std::move(A0));
-    EXPECT_TRUE(&back == &V.back());
-    EXPECT_TRUE(V.size() == 1);
-    EXPECT_TRUE(back.State == ES_Emplaced);
-    EXPECT_TRUE(back.A0.State == EAS_RValue);
-    EXPECT_TRUE(back.A1.State == EAS_Defaulted);
-    EXPECT_TRUE(back.A2.State == EAS_Defaulted);
-    EXPECT_TRUE(back.A3.State == EAS_Defaulted);
+    EXPECT_EQ(&back, &V.back());
+    EXPECT_EQ(V.size(), 1);
+    EXPECT_EQ(back.State, ES_Emplaced);
+    EXPECT_EQ(back.A0.State, EAS_RValue);
+    EXPECT_EQ(back.A1.State, EAS_Defaulted);
+    EXPECT_EQ(back.A2.State, EAS_Defaulted);
+    EXPECT_EQ(back.A3.State, EAS_Defaulted);
   }
   {
     SmallVector<Emplaceable, 3> V;
     Emplaceable &back = V.emplace_back(A0);
-    EXPECT_TRUE(&back == &V.back());
-    EXPECT_TRUE(V.size() == 1);
-    EXPECT_TRUE(back.State == ES_Emplaced);
-    EXPECT_TRUE(back.A0.State == EAS_LValue);
-    EXPECT_TRUE(back.A1.State == EAS_Defaulted);
-    EXPECT_TRUE(back.A2.State == EAS_Defaulted);
-    EXPECT_TRUE(back.A3.State == EAS_Defaulted);
+    EXPECT_EQ(&back, &V.back());
+    EXPECT_EQ(V.size(), 1);
+    EXPECT_EQ(back.State, ES_Emplaced);
+    EXPECT_EQ(back.A0.State, EAS_LValue);
+    EXPECT_EQ(back.A1.State, EAS_Defaulted);
+    EXPECT_EQ(back.A2.State, EAS_Defaulted);
+    EXPECT_EQ(back.A3.State, EAS_Defaulted);
   }
   {
     SmallVector<Emplaceable, 3> V;
     Emplaceable &back = V.emplace_back(A0, A1);
-    EXPECT_TRUE(&back == &V.back());
-    EXPECT_TRUE(V.size() == 1);
-    EXPECT_TRUE(back.State == ES_Emplaced);
-    EXPECT_TRUE(back.A0.State == EAS_LValue);
-    EXPECT_TRUE(back.A1.State == EAS_LValue);
-    EXPECT_TRUE(back.A2.State == EAS_Defaulted);
-    EXPECT_TRUE(back.A3.State == EAS_Defaulted);
+    EXPECT_EQ(&back, &V.back());
+    EXPECT_EQ(V.size(), 1);
+    EXPECT_EQ(back.State, ES_Emplaced);
+    EXPECT_EQ(back.A0.State, EAS_LValue);
+    EXPECT_EQ(back.A1.State, EAS_LValue);
+    EXPECT_EQ(back.A2.State, EAS_Defaulted);
+    EXPECT_EQ(back.A3.State, EAS_Defaulted);
   }
   {
     SmallVector<Emplaceable, 3> V;
     Emplaceable &back = V.emplace_back(std::move(A0), std::move(A1));
-    EXPECT_TRUE(&back == &V.back());
-    EXPECT_TRUE(V.size() == 1);
-    EXPECT_TRUE(back.State == ES_Emplaced);
-    EXPECT_TRUE(back.A0.State == EAS_RValue);
-    EXPECT_TRUE(back.A1.State == EAS_RValue);
-    EXPECT_TRUE(back.A2.State == EAS_Defaulted);
-    EXPECT_TRUE(back.A3.State == EAS_Defaulted);
+    EXPECT_EQ(&back, &V.back());
+    EXPECT_EQ(V.size(), 1);
+    EXPECT_EQ(back.State, ES_Emplaced);
+    EXPECT_EQ(back.A0.State, EAS_RValue);
+    EXPECT_EQ(back.A1.State, EAS_RValue);
+    EXPECT_EQ(back.A2.State, EAS_Defaulted);
+    EXPECT_EQ(back.A3.State, EAS_Defaulted);
   }
   {
     SmallVector<Emplaceable, 3> V;
     Emplaceable &back = V.emplace_back(std::move(A0), A1, std::move(A2), A3);
-    EXPECT_TRUE(&back == &V.back());
-    EXPECT_TRUE(V.size() == 1);
-    EXPECT_TRUE(back.State == ES_Emplaced);
-    EXPECT_TRUE(back.A0.State == EAS_RValue);
-    EXPECT_TRUE(back.A1.State == EAS_LValue);
-    EXPECT_TRUE(back.A2.State == EAS_RValue);
-    EXPECT_TRUE(back.A3.State == EAS_LValue);
+    EXPECT_EQ(&back, &V.back());
+    EXPECT_EQ(V.size(), 1);
+    EXPECT_EQ(back.State, ES_Emplaced);
+    EXPECT_EQ(back.A0.State, EAS_RValue);
+    EXPECT_EQ(back.A1.State, EAS_LValue);
+    EXPECT_EQ(back.A2.State, EAS_RValue);
+    EXPECT_EQ(back.A3.State, EAS_LValue);
   }
   {
     SmallVector<int, 1> V;
@@ -1173,9 +1228,9 @@ TEST(SmallVectorTest, ToVector) {
     // for being/end iterators.
     namespace_with_adl::MyVector V = {{1, 2, 3}};
     auto IntVector = to_vector(V);
-    EXPECT_THAT(IntVector, testing::ElementsAre(1, 2, 3));
+    CHECK_THAT(IntVector, Catch::Matchers::RangeEquals({1, 2, 3}));
     IntVector = to_vector<3>(V);
-    EXPECT_THAT(IntVector, testing::ElementsAre(1, 2, 3));
+    CHECK_THAT(IntVector, Catch::Matchers::RangeEquals({1, 2, 3}));
   }
 }
 
@@ -1240,9 +1295,9 @@ TEST(SmallVectorTest, ToVectorOf) {
     // iterators.
     namespace_with_adl::MyVector V = {{1, 2, 3}};
     auto UnsignedVector = to_vector_of<unsigned>(V);
-    EXPECT_THAT(UnsignedVector, testing::ElementsAre(1u, 2u, 3u));
+    CHECK_THAT(UnsignedVector, Catch::Matchers::RangeEquals({1u, 2u, 3u}));
     UnsignedVector = to_vector_of<unsigned, 3>(V);
-    EXPECT_THAT(UnsignedVector, testing::ElementsAre(1u, 2u, 3u));
+    CHECK_THAT(UnsignedVector, Catch::Matchers::RangeEquals({1u, 2u, 3u}));
   }
 }
 
@@ -1259,9 +1314,7 @@ protected:
     return std::is_same_v<T, typename VectorT::value_type>;
   }
 
-  void SetUp() override {
-    SmallVectorTestBase::SetUp();
-
+  SmallVectorReferenceInvalidationTest() {
     // Fill up the small size so that insertions move the elements.
     for (int I = 0, E = NumBuiltinElts(V); I != E; ++I)
       V.emplace_back(I + 1);
@@ -1271,13 +1324,10 @@ protected:
 // Test one type that's trivially copyable (int) and one that isn't
 // (Constructable) since reference invalidation may be fixed differently for
 // each.
-using SmallVectorReferenceInvalidationTestTypes =
-    ::testing::Types<SmallVector<int, 3>, SmallVector<Constructable, 3>>;
-
-TYPED_TEST_SUITE(SmallVectorReferenceInvalidationTest,
-                 SmallVectorReferenceInvalidationTestTypes, );
+#define WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest \
+  (SmallVector<int, 3>), (SmallVector<Constructable, 3>)
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, PushBack) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest PushBack", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   // Note: setup adds [1, 2, ...] to V until it's at capacity in small mode.
   auto &V = this->V;
   int N = NumBuiltinElts(V);
@@ -1299,7 +1349,7 @@ TYPED_TEST(SmallVectorReferenceInvalidationTest, PushBack) {
   EXPECT_EQ(int(V.size()) - 1, V.back());
 }
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, PushBackMoved) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest PushBackMoved", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   // Note: setup adds [1, 2, ...] to V until it's at capacity in small mode.
   auto &V = this->V;
   int N = NumBuiltinElts(V);
@@ -1328,7 +1378,7 @@ TYPED_TEST(SmallVectorReferenceInvalidationTest, PushBackMoved) {
   }
 }
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, Resize) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest Resize", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   auto &V = this->V;
   (void)V;
   int N = NumBuiltinElts(V);
@@ -1342,7 +1392,7 @@ TYPED_TEST(SmallVectorReferenceInvalidationTest, Resize) {
   EXPECT_EQ(1, V.back());
 }
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, Append) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest Append", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   auto &V = this->V;
   (void)V;
   V.append(1, V.back());
@@ -1358,11 +1408,11 @@ TYPED_TEST(SmallVectorReferenceInvalidationTest, Append) {
   EXPECT_EQ(1, V.back());
 }
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, AppendRange) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest AppendRange", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   auto &V = this->V;
   (void)V;
-#if !defined(NDEBUG) && GTEST_HAS_DEATH_TEST
-  EXPECT_DEATH(V.append(V.begin(), V.begin() + 1), this->AssertionMessage);
+#if !defined(NDEBUG) && WPIUTIL_HAS_DEATH_TEST
+  CHECK_DEATH(V.append(V.begin(), V.begin() + 1), this->AssertionMessage);
 
   ASSERT_EQ(3u, NumBuiltinElts(V));
   ASSERT_EQ(3u, V.size());
@@ -1371,11 +1421,11 @@ TYPED_TEST(SmallVectorReferenceInvalidationTest, AppendRange) {
 
   // Confirm this checks for growth when there's more than one element
   // appended.
-  EXPECT_DEATH(V.append(V.begin(), V.end()), this->AssertionMessage);
+  CHECK_DEATH(V.append(V.begin(), V.end()), this->AssertionMessage);
 #endif
 }
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, Assign) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest Assign", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   // Note: setup adds [1, 2, ...] to V until it's at capacity in small mode.
   auto &V = this->V;
   (void)V;
@@ -1413,17 +1463,17 @@ TYPED_TEST(SmallVectorReferenceInvalidationTest, Assign) {
   EXPECT_EQ(2, V[0]);
 }
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, AssignRange) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest AssignRange", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   auto &V = this->V;
-#if !defined(NDEBUG) && GTEST_HAS_DEATH_TEST
-  EXPECT_DEATH(V.assign(V.begin(), V.end()), this->AssertionMessage);
-  EXPECT_DEATH(V.assign(V.begin(), V.end() - 1), this->AssertionMessage);
+#if !defined(NDEBUG) && WPIUTIL_HAS_DEATH_TEST
+  CHECK_DEATH(V.assign(V.begin(), V.end()), this->AssertionMessage);
+  CHECK_DEATH(V.assign(V.begin(), V.end() - 1), this->AssertionMessage);
 #endif
   V.assign(V.begin(), V.begin());
   EXPECT_TRUE(V.empty());
 }
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, Insert) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest Insert", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   // Note: setup adds [1, 2, ...] to V until it's at capacity in small mode.
   auto &V = this->V;
   (void)V;
@@ -1445,7 +1495,7 @@ TYPED_TEST(SmallVectorReferenceInvalidationTest, Insert) {
   EXPECT_EQ(int(V.size() - 1), V.back());
 }
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, InsertMoved) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest InsertMoved", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   // Note: setup adds [1, 2, ...] to V until it's at capacity in small mode.
   auto &V = this->V;
   (void)V;
@@ -1473,7 +1523,7 @@ TYPED_TEST(SmallVectorReferenceInvalidationTest, InsertMoved) {
   }
 }
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, InsertN) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest InsertN", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   auto &V = this->V;
   (void)V;
 
@@ -1492,12 +1542,11 @@ TYPED_TEST(SmallVectorReferenceInvalidationTest, InsertN) {
   EXPECT_EQ(1, V.front());
 }
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, InsertRange) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest InsertRange", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   auto &V = this->V;
   (void)V;
-#if !defined(NDEBUG) && GTEST_HAS_DEATH_TEST
-  EXPECT_DEATH(V.insert(V.begin(), V.begin(), V.begin() + 1),
-               this->AssertionMessage);
+#if !defined(NDEBUG) && WPIUTIL_HAS_DEATH_TEST
+  CHECK_DEATH(V.insert(V.begin(), V.begin(), V.begin() + 1), this->AssertionMessage);
 
   ASSERT_EQ(3u, NumBuiltinElts(V));
   ASSERT_EQ(3u, V.size());
@@ -1506,11 +1555,11 @@ TYPED_TEST(SmallVectorReferenceInvalidationTest, InsertRange) {
 
   // Confirm this checks for growth when there's more than one element
   // inserted.
-  EXPECT_DEATH(V.insert(V.begin(), V.begin(), V.end()), this->AssertionMessage);
+  CHECK_DEATH(V.insert(V.begin(), V.begin(), V.end()), this->AssertionMessage);
 #endif
 }
 
-TYPED_TEST(SmallVectorReferenceInvalidationTest, EmplaceBack) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorReferenceInvalidationTest, "SmallVectorReferenceInvalidationTest EmplaceBack", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorReferenceInvalidationTest) {
   // Note: setup adds [1, 2, ...] to V until it's at capacity in small mode.
   auto &V = this->V;
   int N = NumBuiltinElts(V);
@@ -1542,24 +1591,19 @@ protected:
 
   VectorT V;
 
-  void SetUp() override {
-    SmallVectorTestBase::SetUp();
-
+  SmallVectorInternalReferenceInvalidationTest() {
     // Fill up the small size so that insertions move the elements.
     for (int I = 0, E = NumBuiltinElts(V); I != E; ++I)
       V.emplace_back(I + 1, I + 1);
   }
 };
 
-// Test pairs of the same types from SmallVectorReferenceInvalidationTestTypes.
-using SmallVectorInternalReferenceInvalidationTestTypes =
-    ::testing::Types<SmallVector<std::pair<int, int>, 3>,
-                     SmallVector<std::pair<Constructable, Constructable>, 3>>;
-
-TYPED_TEST_SUITE(SmallVectorInternalReferenceInvalidationTest,
-                 SmallVectorInternalReferenceInvalidationTestTypes, );
+// Test pairs of the same types from SmallVectorReferenceInvalidationTs.
+#define WPIUTIL_TEST_TYPES_SmallVectorInternalReferenceInvalidationTest \
+  (SmallVector<std::pair<int, int>, 3>), \
+      (SmallVector<std::pair<Constructable, Constructable>, 3>)
 
-TYPED_TEST(SmallVectorInternalReferenceInvalidationTest, EmplaceBack) {
+TEMPLATE_TEST_CASE_METHOD(SmallVectorInternalReferenceInvalidationTest, "SmallVectorInternalReferenceInvalidationTest EmplaceBack", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_SmallVectorInternalReferenceInvalidationTest) {
   // Note: setup adds [1, 2, ...] to V until it's at capacity in small mode.
   auto &V = this->V;
   int N = NumBuiltinElts(V);
diff --git a/llvm/unittests/Support/ConvertUTFTest.cpp b/llvm/unittests/Support/ConvertUTFTest.cpp
index eb17a06c4369c9486c57b61f519a7429d9ef3d80..d47c5cc58362c14aeb639b67cd6f539525bb9729 100644
--- a/llvm/unittests/Support/ConvertUTFTest.cpp
+++ b/llvm/unittests/Support/ConvertUTFTest.cpp
@@ -7,9 +7,13 @@
 //===----------------------------------------------------------------------===//
 
 #include "llvm/Support/ConvertUTF.h"
-#include "llvm/Support/SmallString.h"
-#include "llvm/Support/SmallVector.h"
-#include "gtest/gtest.h"
+#include "llvm/ADT/SmallString.h"
+#include "llvm/ADT/SmallVector.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 #include <string>
 #include <vector>
 
@@ -206,9 +210,8 @@ ConvertUTF8ToUnicodeScalarsPartialLenient(std::string_view S) {
   return std::make_pair(ErrorCode, Decoded);
 }
 
-::testing::AssertionResult
-CheckConvertUTF8ToUnicodeScalars(ConvertUTFResultContainer Expected,
-                                 std::string_view S, bool Partial = false) {
+bool CheckConvertUTF8ToUnicodeScalars(ConvertUTFResultContainer Expected,
+                                      std::string_view S, bool Partial = false) {
   ConversionResult ErrorCode;
   std::vector<unsigned> Decoded;
   if (!Partial)
@@ -216,18 +219,20 @@ CheckConvertUTF8ToUnicodeScalars(ConvertUTFResultContainer Expected,
   else
     std::tie(ErrorCode, Decoded) = ConvertUTF8ToUnicodeScalarsPartialLenient(S);
 
-  if (Expected.ErrorCode != ErrorCode)
-    return ::testing::AssertionFailure() << "Expected error code "
-                                         << Expected.ErrorCode << ", actual "
-                                         << ErrorCode;
+  if (Expected.ErrorCode != ErrorCode) {
+    SCOPED_TRACE("Expected error code " << Expected.ErrorCode << ", actual "
+                                         << ErrorCode);
+    return false;
+  }
 
-  if (Expected.UnicodeScalars != Decoded)
-    return ::testing::AssertionFailure()
-           << "Expected lenient decoded result:\n"
-           << ::testing::PrintToString(Expected.UnicodeScalars) << "\n"
-           << "Actual result:\n" << ::testing::PrintToString(Decoded);
+  if (Expected.UnicodeScalars != Decoded) {
+    SCOPED_TRACE("Expected lenient decoded result: "
+                  << Catch::Detail::stringify(Expected.UnicodeScalars)
+                  << "\nActual result: " << Catch::Detail::stringify(Decoded));
+    return false;
+  }
 
-  return ::testing::AssertionSuccess();
+  return true;
 }
 
 TEST(ConvertUTFTest, UTF8ToUTF32Lenient) {
@@ -1734,4 +1739,3 @@ TEST(ConvertUTFTest, UTF8ToUTF32PartialLenient) {
       ConvertUTFResultContainer(sourceExhausted).withScalars(0x0041),
       "\x41\xc2", true));
 }
-
diff --git a/llvm/unittests/Support/EndianTest.cpp b/llvm/unittests/Support/EndianTest.cpp
index d707fdb55c7c97b21310b48bf3c23e328dd644aa..3d8445808f54fb811b471a0fb2f6884ac6dffe3f 100644
--- a/llvm/unittests/Support/EndianTest.cpp
+++ b/llvm/unittests/Support/EndianTest.cpp
@@ -7,8 +7,11 @@
 //===----------------------------------------------------------------------===//
 
 #include "llvm/Support/Endian.h"
-#include "llvm/Support/DataTypes.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 #include <cstdlib>
 #include <ctime>
 using namespace llvm;
@@ -23,15 +26,14 @@ TEST(Endian, Read) {
   unsigned char bigval[] = {0x00, 0x01, 0x02, 0x03, 0x04};
   unsigned char littleval[] = {0x00, 0x04, 0x03, 0x02, 0x01};
   int32_t BigAsHost = 0x00010203;
-  EXPECT_EQ(BigAsHost,
-            (endian::read<int32_t, unaligned>(bigval, std::endian::big)));
+  EXPECT_EQ(BigAsHost, (endian::read<int32_t, unaligned>(bigval, std::endian::big)));
   int32_t LittleAsHost = 0x02030400;
   EXPECT_EQ(LittleAsHost, (endian::read<int32_t, unaligned>(
                               littleval, std::endian::little)));
 
-  EXPECT_EQ(
-      (endian::read<int32_t, unaligned>(bigval + 1, std::endian::big)),
-      (endian::read<int32_t, unaligned>(littleval + 1, std::endian::little)));
+  CHECK(endian::read<int32_t, unaligned>(bigval + 1, std::endian::big) ==
+        endian::read<int32_t, unaligned>(littleval + 1,
+                                         std::endian::little));
 }
 
 TEST(Endian, WriteNext) {
@@ -61,45 +63,32 @@ TEST(Endian, ReadBitAligned) {
   // Simple test to make sure we properly pull out the 0x0 word.
   unsigned char littleval[] = {0x3f, 0x00, 0x00, 0x00, 0xc0, 0xff, 0xff, 0xff};
   unsigned char bigval[] = {0x00, 0x00, 0x00, 0x3f, 0xff, 0xff, 0xff, 0xc0};
-  EXPECT_EQ(
-      (endian::readAtBitAlignment<int, std::endian::little, unaligned>(
-          &littleval[0], 6)),
-      0x0);
-  EXPECT_EQ((endian::readAtBitAlignment<int, std::endian::big, unaligned>(
-                &bigval[0], 6)),
-            0x0);
+  CHECK(endian::readAtBitAlignment<int, std::endian::little, unaligned>(
+            &littleval[0], 6) == 0x0);
+  CHECK(endian::readAtBitAlignment<int, std::endian::big, unaligned>(
+            &bigval[0], 6) == 0x0);
   // Test to make sure that signed right shift of 0xf0000000 is masked
   // properly.
   unsigned char littleval2[] = {0x00, 0x00, 0x00, 0xf0, 0x00, 0x00, 0x00, 0x00};
   unsigned char bigval2[] = {0xf0, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
-  EXPECT_EQ(
-      (endian::readAtBitAlignment<int, std::endian::little, unaligned>(
-          &littleval2[0], 4)),
-      0x0f000000);
-  EXPECT_EQ((endian::readAtBitAlignment<int, std::endian::big, unaligned>(
-                &bigval2[0], 4)),
-            0x0f000000);
+  CHECK(endian::readAtBitAlignment<int, std::endian::little, unaligned>(
+            &littleval2[0], 4) == 0x0f000000);
+  CHECK(endian::readAtBitAlignment<int, std::endian::big, unaligned>(
+            &bigval2[0], 4) == 0x0f000000);
   // Test to make sure left shift of start bit doesn't overflow.
-  EXPECT_EQ(
-      (endian::readAtBitAlignment<int, std::endian::little, unaligned>(
-          &littleval2[0], 1)),
-      0x78000000);
-  EXPECT_EQ((endian::readAtBitAlignment<int, std::endian::big, unaligned>(
-                &bigval2[0], 1)),
-            0x78000000);
+  CHECK(endian::readAtBitAlignment<int, std::endian::little, unaligned>(
+            &littleval2[0], 1) == 0x78000000);
+  CHECK(endian::readAtBitAlignment<int, std::endian::big, unaligned>(
+            &bigval2[0], 1) == 0x78000000);
   // Test to make sure 64-bit int doesn't overflow.
   unsigned char littleval3[] = {0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0xf0,
                                 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
   unsigned char bigval3[] = {0xf0, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
                              0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
-  EXPECT_EQ(
-      (endian::readAtBitAlignment<int64_t, std::endian::little, unaligned>(
-          &littleval3[0], 4)),
-      0x0f00000000000000);
-  EXPECT_EQ(
-      (endian::readAtBitAlignment<int64_t, std::endian::big, unaligned>(
-          &bigval3[0], 4)),
-      0x0f00000000000000);
+  CHECK(endian::readAtBitAlignment<int64_t, std::endian::little, unaligned>(
+            &littleval3[0], 4) == 0x0f00000000000000);
+  CHECK(endian::readAtBitAlignment<int64_t, std::endian::big, unaligned>(
+            &bigval3[0], 4) == 0x0f00000000000000);
 }
 
 TEST(Endian, WriteBitAligned) {
diff --git a/llvm/unittests/Support/ErrnoTest.cpp b/llvm/unittests/Support/ErrnoTest.cpp
index 747d9dde0b73752e53c23e8fc750a3a0c64743fb..b0eb3234ab68694dbdf002ff3af5662556b2827d 100644
--- a/llvm/unittests/Support/ErrnoTest.cpp
+++ b/llvm/unittests/Support/ErrnoTest.cpp
@@ -7,7 +7,11 @@
 //===----------------------------------------------------------------------===//
 
 #include "llvm/Support/Errno.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 
 using namespace llvm::sys;
 
diff --git a/llvm/unittests/Support/MathExtrasTest.cpp b/llvm/unittests/Support/MathExtrasTest.cpp
index 5cb85fc55da92c75dbe431721e44b99594b162dd..1ea3df25fd03bbb3912176dcf5e75abebb1d4674 100644
--- a/llvm/unittests/Support/MathExtrasTest.cpp
+++ b/llvm/unittests/Support/MathExtrasTest.cpp
@@ -7,7 +7,11 @@
 //===----------------------------------------------------------------------===//
 
 #include "llvm/Support/MathExtras.h"
-#include "gtest/gtest.h"
+#include <catch2/catch_test_macros.hpp>
+#include <catch2/matchers/catch_matchers_floating_point.hpp>
+#include <catch2/matchers/catch_matchers_range_equals.hpp>
+#include <catch2/matchers/catch_matchers_vector.hpp>
+#include <catch2/catch_template_test_macros.hpp>
 #include <limits>
 
 using namespace llvm;
@@ -153,22 +157,22 @@ TEST(MathExtras, PowerOf2Ceil) {
 }
 
 TEST(MathExtras, ConstantLog2) {
-  EXPECT_EQ(ConstantLog2<1ULL << 0>(), 0U);
-  EXPECT_EQ(ConstantLog2<1ULL << 1>(), 1U);
-  EXPECT_EQ(ConstantLog2<1ULL << 2>(), 2U);
-  EXPECT_EQ(ConstantLog2<1ULL << 3>(), 3U);
-  EXPECT_EQ(ConstantLog2<1ULL << 4>(), 4U);
-  EXPECT_EQ(ConstantLog2<1ULL << 5>(), 5U);
-  EXPECT_EQ(ConstantLog2<1ULL << 6>(), 6U);
-  EXPECT_EQ(ConstantLog2<1ULL << 7>(), 7U);
-  EXPECT_EQ(ConstantLog2<1ULL << 8>(), 8U);
-  EXPECT_EQ(ConstantLog2<1ULL << 9>(), 9U);
-  EXPECT_EQ(ConstantLog2<1ULL << 10>(), 10U);
-  EXPECT_EQ(ConstantLog2<1ULL << 11>(), 11U);
-  EXPECT_EQ(ConstantLog2<1ULL << 12>(), 12U);
-  EXPECT_EQ(ConstantLog2<1ULL << 13>(), 13U);
-  EXPECT_EQ(ConstantLog2<1ULL << 14>(), 14U);
-  EXPECT_EQ(ConstantLog2<1ULL << 15>(), 15U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 0>() == 0U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 1>() == 1U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 2>() == 2U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 3>() == 3U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 4>() == 4U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 5>() == 5U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 6>() == 6U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 7>() == 7U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 8>() == 8U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 9>() == 9U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 10>() == 10U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 11>() == 11U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 12>() == 12U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 13>() == 13U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 14>() == 14U);
+  EXPECT_TRUE(ConstantLog2<1ULL << 15>() == 15U);
 }
 
 TEST(MathExtras, MinAlign) {
@@ -194,15 +198,13 @@ TEST(MathExtras, AlignTo) {
   EXPECT_EQ(8u, alignTo<8>(5));
   EXPECT_EQ(24u, alignTo<8>(17));
   EXPECT_EQ(0u, alignTo<8>(~0LL));
-  EXPECT_EQ(254u,
-            alignTo<static_cast<uint8_t>(127)>(static_cast<uint8_t>(200)));
+  EXPECT_EQ(254u, alignTo<static_cast<uint8_t>(127)>(static_cast<uint8_t>(200)));
 
   EXPECT_EQ(7u, alignTo(5, 8, 7));
   EXPECT_EQ(17u, alignTo(17, 8, 1));
   EXPECT_EQ(3u, alignTo(~0LL, 8, 3));
   EXPECT_EQ(552u, alignTo(321, 255, 42));
-  EXPECT_EQ(std::numeric_limits<uint32_t>::max(),
-            alignTo(std::numeric_limits<uint32_t>::max(), 2, 1));
+  EXPECT_EQ(std::numeric_limits<uint32_t>::max(), alignTo(std::numeric_limits<uint32_t>::max(), 2, 1));
 
   // Overflow.
   EXPECT_EQ(0u, alignTo(static_cast<uint8_t>(200), static_cast<uint8_t>(128)));
@@ -228,8 +230,7 @@ TEST(MathExtras, AlignToPowerOf2) {
 TEST(MathExtras, AlignDown) {
   EXPECT_EQ(0u, alignDown(5, 8));
   EXPECT_EQ(16u, alignDown(17, 8));
-  EXPECT_EQ(std::numeric_limits<uint32_t>::max() - 1,
-            alignDown(std::numeric_limits<uint32_t>::max(), 2));
+  EXPECT_EQ(std::numeric_limits<uint32_t>::max() - 1, alignDown(std::numeric_limits<uint32_t>::max(), 2));
 }
 
 template <typename T> void SaturatingAddTestHelper() {
@@ -435,41 +436,41 @@ TEST(MathExtras, SaturatingMultiplyAdd) {
 }
 
 TEST(MathExtras, IsShiftedUInt) {
-  EXPECT_TRUE((isShiftedUInt<1, 0>(0)));
-  EXPECT_TRUE((isShiftedUInt<1, 0>(1)));
-  EXPECT_FALSE((isShiftedUInt<1, 0>(2)));
-  EXPECT_FALSE((isShiftedUInt<1, 0>(3)));
-  EXPECT_FALSE((isShiftedUInt<1, 0>(0x8000000000000000)));
-  EXPECT_TRUE((isShiftedUInt<1, 63>(0x8000000000000000)));
-  EXPECT_TRUE((isShiftedUInt<2, 62>(0xC000000000000000)));
-  EXPECT_FALSE((isShiftedUInt<2, 62>(0xE000000000000000)));
+  CHECK(isShiftedUInt < 1, 0>(0));
+  CHECK(isShiftedUInt < 1, 0>(1));
+  CHECK_FALSE(isShiftedUInt < 1, 0>(2));
+  CHECK_FALSE(isShiftedUInt < 1, 0>(3));
+  CHECK_FALSE(isShiftedUInt < 1, 0>(0x8000000000000000));
+  CHECK(isShiftedUInt < 1, 63>(0x8000000000000000));
+  CHECK(isShiftedUInt < 2, 62>(0xC000000000000000));
+  CHECK_FALSE(isShiftedUInt < 2, 62>(0xE000000000000000));
 
   // 0x201 is ten bits long and has a 1 in the MSB and LSB.
-  EXPECT_TRUE((isShiftedUInt<10, 5>(uint64_t(0x201) << 5)));
-  EXPECT_FALSE((isShiftedUInt<10, 5>(uint64_t(0x201) << 4)));
-  EXPECT_FALSE((isShiftedUInt<10, 5>(uint64_t(0x201) << 6)));
+  CHECK(isShiftedUInt < 10, 5>(uint64_t(0x201) << 5));
+  CHECK_FALSE(isShiftedUInt < 10, 5>(uint64_t(0x201) << 4));
+  CHECK_FALSE(isShiftedUInt < 10, 5>(uint64_t(0x201) << 6));
 }
 
 TEST(MathExtras, IsShiftedInt) {
-  EXPECT_TRUE((isShiftedInt<1, 0>(0)));
-  EXPECT_TRUE((isShiftedInt<1, 0>(-1)));
-  EXPECT_FALSE((isShiftedInt<1, 0>(2)));
-  EXPECT_FALSE((isShiftedInt<1, 0>(3)));
-  EXPECT_FALSE((isShiftedInt<1, 0>(0x8000000000000000)));
-  EXPECT_TRUE((isShiftedInt<1, 63>(0x8000000000000000)));
-  EXPECT_TRUE((isShiftedInt<2, 62>(0xC000000000000000)));
-  EXPECT_FALSE((isShiftedInt<2, 62>(0xE000000000000000)));
+  CHECK(isShiftedInt < 1, 0>(0));
+  CHECK(isShiftedInt < 1, 0>(-1));
+  CHECK_FALSE(isShiftedInt < 1, 0>(2));
+  CHECK_FALSE(isShiftedInt < 1, 0>(3));
+  CHECK_FALSE(isShiftedInt < 1, 0>(0x8000000000000000));
+  CHECK(isShiftedInt < 1, 63>(0x8000000000000000));
+  CHECK(isShiftedInt < 2, 62>(0xC000000000000000));
+  CHECK_FALSE(isShiftedInt < 2, 62>(0xE000000000000000));
 
   // 0x201 is ten bits long and has a 1 in the MSB and LSB.
-  EXPECT_TRUE((isShiftedInt<11, 5>(int64_t(0x201) << 5)));
-  EXPECT_FALSE((isShiftedInt<11, 5>(int64_t(0x201) << 3)));
-  EXPECT_FALSE((isShiftedInt<11, 5>(int64_t(0x201) << 6)));
-  EXPECT_TRUE((isShiftedInt<11, 5>(-(int64_t(0x201) << 5))));
-  EXPECT_FALSE((isShiftedInt<11, 5>(-(int64_t(0x201) << 3))));
-  EXPECT_FALSE((isShiftedInt<11, 5>(-(int64_t(0x201) << 6))));
+  CHECK(isShiftedInt < 11, 5>(int64_t(0x201) << 5));
+  CHECK_FALSE(isShiftedInt < 11, 5>(int64_t(0x201) << 3));
+  CHECK_FALSE(isShiftedInt < 11, 5>(int64_t(0x201) << 6));
+  CHECK(isShiftedInt < 11, 5>(-(int64_t(0x201) << 5)));
+  CHECK_FALSE(isShiftedInt < 11, 5>(-(int64_t(0x201) << 3)));
+  CHECK_FALSE(isShiftedInt < 11, 5>(-(int64_t(0x201) << 6)));
 
-  EXPECT_TRUE((isShiftedInt<6, 10>(-(int64_t(1) << 15))));
-  EXPECT_FALSE((isShiftedInt<6, 10>(int64_t(1) << 15)));
+  CHECK(isShiftedInt < 6, 10>(-(int64_t(1) << 15)));
+  CHECK_FALSE(isShiftedInt < 6, 10>(int64_t(1) << 15));
 }
 
 TEST(MathExtras, DivideNearest) {
@@ -481,15 +482,11 @@ TEST(MathExtras, DivideNearest) {
   EXPECT_EQ(divideNearest(3, 1), 3u);
   EXPECT_EQ(divideNearest(3, 6), 1u);
   EXPECT_EQ(divideNearest(3, 7), 0u);
-  EXPECT_EQ(divideNearest(std::numeric_limits<uint32_t>::max(), 2),
-            std::numeric_limits<uint32_t>::max() / 2 + 1);
-  EXPECT_EQ(divideNearest(std::numeric_limits<uint64_t>::max(), 2),
-            std::numeric_limits<uint64_t>::max() / 2 + 1);
-  EXPECT_EQ(divideNearest(std::numeric_limits<uint64_t>::max(), 1),
-            std::numeric_limits<uint64_t>::max());
+  EXPECT_EQ(divideNearest(std::numeric_limits<uint32_t>::max(), 2), std::numeric_limits<uint32_t>::max() / 2 + 1);
+  EXPECT_EQ(divideNearest(std::numeric_limits<uint64_t>::max(), 2), std::numeric_limits<uint64_t>::max() / 2 + 1);
+  EXPECT_EQ(divideNearest(std::numeric_limits<uint64_t>::max(), 1), std::numeric_limits<uint64_t>::max());
   EXPECT_EQ(divideNearest(std::numeric_limits<uint64_t>::max() - 1,
-                          std::numeric_limits<uint64_t>::max()),
-            1u);
+                          std::numeric_limits<uint64_t>::max()), 1u);
 }
 
 TEST(MathExtras, DivideCeil) {
@@ -501,12 +498,9 @@ TEST(MathExtras, DivideCeil) {
   EXPECT_EQ(divideCeil(3, 1), 3u);
   EXPECT_EQ(divideCeil(3, 6), 1u);
   EXPECT_EQ(divideCeil(3, 7), 1u);
-  EXPECT_EQ(divideCeil(std::numeric_limits<uint32_t>::max(), 2),
-            std::numeric_limits<uint32_t>::max() / 2 + 1);
-  EXPECT_EQ(divideCeil(std::numeric_limits<uint64_t>::max(), 2),
-            std::numeric_limits<uint64_t>::max() / 2 + 1);
-  EXPECT_EQ(divideCeil(std::numeric_limits<uint64_t>::max(), 1),
-            std::numeric_limits<uint64_t>::max());
+  EXPECT_EQ(divideCeil(std::numeric_limits<uint32_t>::max(), 2), std::numeric_limits<uint32_t>::max() / 2 + 1);
+  EXPECT_EQ(divideCeil(std::numeric_limits<uint64_t>::max(), 2), std::numeric_limits<uint64_t>::max() / 2 + 1);
+  EXPECT_EQ(divideCeil(std::numeric_limits<uint64_t>::max(), 1), std::numeric_limits<uint64_t>::max());
 
   EXPECT_EQ(divideCeilSigned(14, 3), 5);
   EXPECT_EQ(divideCeilSigned(15, 3), 5);
@@ -516,22 +510,15 @@ TEST(MathExtras, DivideCeil) {
   EXPECT_EQ(divideCeilSigned(-15, 3), -5);
   EXPECT_EQ(divideCeilSigned(0, 3), 0);
   EXPECT_EQ(divideCeilSigned(0, -3), 0);
-  EXPECT_EQ(divideCeilSigned(std::numeric_limits<int32_t>::max(), 2),
-            std::numeric_limits<int32_t>::max() / 2 + 1);
-  EXPECT_EQ(divideCeilSigned(std::numeric_limits<int64_t>::max(), 2),
-            std::numeric_limits<int64_t>::max() / 2 + 1);
-  EXPECT_EQ(divideCeilSigned(std::numeric_limits<int32_t>::max(), -2),
-            std::numeric_limits<int32_t>::min() / 2 + 1);
-  EXPECT_EQ(divideCeilSigned(std::numeric_limits<int64_t>::max(), -2),
-            std::numeric_limits<int64_t>::min() / 2 + 1);
-  EXPECT_EQ(divideCeilSigned(std::numeric_limits<int64_t>::min(), 1),
-            std::numeric_limits<int64_t>::min());
+  EXPECT_EQ(divideCeilSigned(std::numeric_limits<int32_t>::max(), 2), std::numeric_limits<int32_t>::max() / 2 + 1);
+  EXPECT_EQ(divideCeilSigned(std::numeric_limits<int64_t>::max(), 2), std::numeric_limits<int64_t>::max() / 2 + 1);
+  EXPECT_EQ(divideCeilSigned(std::numeric_limits<int32_t>::max(), -2), std::numeric_limits<int32_t>::min() / 2 + 1);
+  EXPECT_EQ(divideCeilSigned(std::numeric_limits<int64_t>::max(), -2), std::numeric_limits<int64_t>::min() / 2 + 1);
+  EXPECT_EQ(divideCeilSigned(std::numeric_limits<int64_t>::min(), 1), std::numeric_limits<int64_t>::min());
 
   // Overflow.
-  EXPECT_TRUE(
-      divideSignedWouldOverflow(std::numeric_limits<int8_t>::min(), -1));
-  EXPECT_TRUE(
-      divideSignedWouldOverflow(std::numeric_limits<int64_t>::min(), -1));
+  EXPECT_TRUE(divideSignedWouldOverflow(std::numeric_limits<int8_t>::min(), -1));
+  EXPECT_TRUE(divideSignedWouldOverflow(std::numeric_limits<int64_t>::min(), -1));
 }
 
 TEST(MathExtras, DivideFloorSigned) {
@@ -543,16 +530,11 @@ TEST(MathExtras, DivideFloorSigned) {
   EXPECT_EQ(divideFloorSigned(-15, 3), -5);
   EXPECT_EQ(divideFloorSigned(0, 3), 0);
   EXPECT_EQ(divideFloorSigned(0, -3), 0);
-  EXPECT_EQ(divideFloorSigned(std::numeric_limits<int32_t>::max(), 2),
-            std::numeric_limits<int32_t>::max() / 2);
-  EXPECT_EQ(divideFloorSigned(std::numeric_limits<int64_t>::max(), 2),
-            std::numeric_limits<int64_t>::max() / 2);
-  EXPECT_EQ(divideFloorSigned(std::numeric_limits<int32_t>::max(), -2),
-            std::numeric_limits<int32_t>::min() / 2);
-  EXPECT_EQ(divideFloorSigned(std::numeric_limits<int64_t>::max(), -2),
-            std::numeric_limits<int64_t>::min() / 2);
-  EXPECT_EQ(divideFloorSigned(std::numeric_limits<int64_t>::min(), 1),
-            std::numeric_limits<int64_t>::min());
+  EXPECT_EQ(divideFloorSigned(std::numeric_limits<int32_t>::max(), 2), std::numeric_limits<int32_t>::max() / 2);
+  EXPECT_EQ(divideFloorSigned(std::numeric_limits<int64_t>::max(), 2), std::numeric_limits<int64_t>::max() / 2);
+  EXPECT_EQ(divideFloorSigned(std::numeric_limits<int32_t>::max(), -2), std::numeric_limits<int32_t>::min() / 2);
+  EXPECT_EQ(divideFloorSigned(std::numeric_limits<int64_t>::max(), -2), std::numeric_limits<int64_t>::min() / 2);
+  EXPECT_EQ(divideFloorSigned(std::numeric_limits<int64_t>::min(), 1), std::numeric_limits<int64_t>::min());
 
   // Same overflow condition, divideSignedWouldOverflow, applies.
 }
@@ -567,129 +549,127 @@ TEST(MathExtras, Mod) {
   EXPECT_EQ(mod(0, 3), 0);
 }
 
-template <typename T> class OverflowTest : public ::testing::Test {};
+#define WPIUTIL_TEST_TYPES_OverflowTest signed char, short, int, long, long long
 
-using OverflowTestTypes = ::testing::Types<signed char, short, int, long,
-                                           long long>;
+template <typename T>
+class OverflowTest {};
 
-TYPED_TEST_SUITE(OverflowTest, OverflowTestTypes, );
-
-TYPED_TEST(OverflowTest, AddNoOverflow) {
-  TypeParam Result;
-  EXPECT_FALSE(AddOverflow<TypeParam>(1, 2, Result));
-  EXPECT_EQ(Result, TypeParam(3));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest AddNoOverflow", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  EXPECT_FALSE(AddOverflow < TestType>(1, 2, Result));
+  EXPECT_EQ(Result, TestType(3));
 }
 
-TYPED_TEST(OverflowTest, AddOverflowToNegative) {
-  TypeParam Result;
-  auto MaxValue = std::numeric_limits<TypeParam>::max();
-  EXPECT_TRUE(AddOverflow<TypeParam>(MaxValue, MaxValue, Result));
-  EXPECT_EQ(Result, TypeParam(-2));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest AddOverflowToNegative", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  auto MaxValue = std::numeric_limits<TestType>::max();
+  EXPECT_TRUE(AddOverflow < TestType>(MaxValue, MaxValue, Result));
+  EXPECT_EQ(Result, TestType(-2));
 }
 
-TYPED_TEST(OverflowTest, AddOverflowToMin) {
-  TypeParam Result;
-  auto MaxValue = std::numeric_limits<TypeParam>::max();
-  EXPECT_TRUE(AddOverflow<TypeParam>(MaxValue, TypeParam(1), Result));
-  EXPECT_EQ(Result, std::numeric_limits<TypeParam>::min());
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest AddOverflowToMin", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  auto MaxValue = std::numeric_limits<TestType>::max();
+  EXPECT_TRUE(AddOverflow < TestType>(MaxValue, TestType(1), Result));
+  EXPECT_EQ(Result, std::numeric_limits<TestType>::min());
 }
 
-TYPED_TEST(OverflowTest, AddOverflowToZero) {
-  TypeParam Result;
-  auto MinValue = std::numeric_limits<TypeParam>::min();
-  EXPECT_TRUE(AddOverflow<TypeParam>(MinValue, MinValue, Result));
-  EXPECT_EQ(Result, TypeParam(0));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest AddOverflowToZero", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  auto MinValue = std::numeric_limits<TestType>::min();
+  EXPECT_TRUE(AddOverflow < TestType>(MinValue, MinValue, Result));
+  EXPECT_EQ(Result, TestType(0));
 }
 
-TYPED_TEST(OverflowTest, AddOverflowToMax) {
-  TypeParam Result;
-  auto MinValue = std::numeric_limits<TypeParam>::min();
-  EXPECT_TRUE(AddOverflow<TypeParam>(MinValue, TypeParam(-1), Result));
-  EXPECT_EQ(Result, std::numeric_limits<TypeParam>::max());
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest AddOverflowToMax", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  auto MinValue = std::numeric_limits<TestType>::min();
+  EXPECT_TRUE(AddOverflow < TestType>(MinValue, TestType(-1), Result));
+  EXPECT_EQ(Result, std::numeric_limits<TestType>::max());
 }
 
-TYPED_TEST(OverflowTest, SubNoOverflow) {
-  TypeParam Result;
-  EXPECT_FALSE(SubOverflow<TypeParam>(1, 2, Result));
-  EXPECT_EQ(Result, TypeParam(-1));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest SubNoOverflow", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  EXPECT_FALSE(SubOverflow < TestType>(1, 2, Result));
+  EXPECT_EQ(Result, TestType(-1));
 }
 
-TYPED_TEST(OverflowTest, SubOverflowToMax) {
-  TypeParam Result;
-  auto MinValue = std::numeric_limits<TypeParam>::min();
-  EXPECT_TRUE(SubOverflow<TypeParam>(0, MinValue, Result));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest SubOverflowToMax", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  auto MinValue = std::numeric_limits<TestType>::min();
+  EXPECT_TRUE(SubOverflow < TestType>(0, MinValue, Result));
   EXPECT_EQ(Result, MinValue);
 }
 
-TYPED_TEST(OverflowTest, SubOverflowToMin) {
-  TypeParam Result;
-  auto MinValue = std::numeric_limits<TypeParam>::min();
-  EXPECT_TRUE(SubOverflow<TypeParam>(0, MinValue, Result));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest SubOverflowToMin", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  auto MinValue = std::numeric_limits<TestType>::min();
+  EXPECT_TRUE(SubOverflow < TestType>(0, MinValue, Result));
   EXPECT_EQ(Result, MinValue);
 }
 
-TYPED_TEST(OverflowTest, SubOverflowToNegative) {
-  TypeParam Result;
-  auto MaxValue = std::numeric_limits<TypeParam>::max();
-  auto MinValue = std::numeric_limits<TypeParam>::min();
-  EXPECT_TRUE(SubOverflow<TypeParam>(MaxValue, MinValue, Result));
-  EXPECT_EQ(Result, TypeParam(-1));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest SubOverflowToNegative", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  auto MaxValue = std::numeric_limits<TestType>::max();
+  auto MinValue = std::numeric_limits<TestType>::min();
+  EXPECT_TRUE(SubOverflow < TestType>(MaxValue, MinValue, Result));
+  EXPECT_EQ(Result, TestType(-1));
 }
 
-TYPED_TEST(OverflowTest, SubOverflowToPositive) {
-  TypeParam Result;
-  auto MaxValue = std::numeric_limits<TypeParam>::max();
-  auto MinValue = std::numeric_limits<TypeParam>::min();
-  EXPECT_TRUE(SubOverflow<TypeParam>(MinValue, MaxValue, Result));
-  EXPECT_EQ(Result, TypeParam(1));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest SubOverflowToPositive", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  auto MaxValue = std::numeric_limits<TestType>::max();
+  auto MinValue = std::numeric_limits<TestType>::min();
+  EXPECT_TRUE(SubOverflow < TestType>(MinValue, MaxValue, Result));
+  EXPECT_EQ(Result, TestType(1));
 }
 
-TYPED_TEST(OverflowTest, MulNoOverflow) {
-  TypeParam Result;
-  EXPECT_FALSE(MulOverflow<TypeParam>(1, 2, Result));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest MulNoOverflow", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  EXPECT_FALSE(MulOverflow < TestType>(1, 2, Result));
   EXPECT_EQ(Result, 2);
-  EXPECT_FALSE(MulOverflow<TypeParam>(-1, 3, Result));
+  EXPECT_FALSE(MulOverflow < TestType>(-1, 3, Result));
   EXPECT_EQ(Result, -3);
-  EXPECT_FALSE(MulOverflow<TypeParam>(4, -2, Result));
+  EXPECT_FALSE(MulOverflow < TestType>(4, -2, Result));
   EXPECT_EQ(Result, -8);
-  EXPECT_FALSE(MulOverflow<TypeParam>(-6, -5, Result));
+  EXPECT_FALSE(MulOverflow < TestType>(-6, -5, Result));
   EXPECT_EQ(Result, 30);
 }
 
-TYPED_TEST(OverflowTest, MulNoOverflowToMax) {
-  TypeParam Result;
-  auto MaxValue = std::numeric_limits<TypeParam>::max();
-  auto MinValue = std::numeric_limits<TypeParam>::min();
-  EXPECT_FALSE(MulOverflow<TypeParam>(MinValue + 1, -1, Result));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest MulNoOverflowToMax", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  auto MaxValue = std::numeric_limits<TestType>::max();
+  auto MinValue = std::numeric_limits<TestType>::min();
+  EXPECT_FALSE(MulOverflow < TestType>(MinValue + 1, -1, Result));
   EXPECT_EQ(Result, MaxValue);
 }
 
-TYPED_TEST(OverflowTest, MulOverflowToMin) {
-  TypeParam Result;
-  auto MinValue = std::numeric_limits<TypeParam>::min();
-  EXPECT_TRUE(MulOverflow<TypeParam>(MinValue, -1, Result));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest MulOverflowToMin", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  auto MinValue = std::numeric_limits<TestType>::min();
+  EXPECT_TRUE(MulOverflow < TestType>(MinValue, -1, Result));
   EXPECT_EQ(Result, MinValue);
 }
 
-TYPED_TEST(OverflowTest, MulOverflowMax) {
-  TypeParam Result;
-  auto MinValue = std::numeric_limits<TypeParam>::min();
-  auto MaxValue = std::numeric_limits<TypeParam>::max();
-  EXPECT_TRUE(MulOverflow<TypeParam>(MinValue, MinValue, Result));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest MulOverflowMax", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  auto MinValue = std::numeric_limits<TestType>::min();
+  auto MaxValue = std::numeric_limits<TestType>::max();
+  EXPECT_TRUE(MulOverflow < TestType>(MinValue, MinValue, Result));
   EXPECT_EQ(Result, 0);
-  EXPECT_TRUE(MulOverflow<TypeParam>(MaxValue, MaxValue, Result));
+  EXPECT_TRUE(MulOverflow < TestType>(MaxValue, MaxValue, Result));
   EXPECT_EQ(Result, 1);
 }
 
-TYPED_TEST(OverflowTest, MulResultZero) {
-  TypeParam Result;
-  EXPECT_FALSE(MulOverflow<TypeParam>(4, 0, Result));
-  EXPECT_EQ(Result, TypeParam(0));
-  EXPECT_FALSE(MulOverflow<TypeParam>(-5, 0, Result));
-  EXPECT_EQ(Result, TypeParam(0));
-  EXPECT_FALSE(MulOverflow<TypeParam>(0, 5, Result));
-  EXPECT_EQ(Result, TypeParam(0));
-  EXPECT_FALSE(MulOverflow<TypeParam>(0, -5, Result));
-  EXPECT_EQ(Result, TypeParam(0));
+TEMPLATE_TEST_CASE_METHOD(OverflowTest, "OverflowTest MulResultZero", "[wpiutil][llvm]", WPIUTIL_TEST_TYPES_OverflowTest) {
+  TestType Result;
+  EXPECT_FALSE(MulOverflow < TestType>(4, 0, Result));
+  EXPECT_EQ(Result, TestType(0));
+  EXPECT_FALSE(MulOverflow < TestType>(-5, 0, Result));
+  EXPECT_EQ(Result, TestType(0));
+  EXPECT_FALSE(MulOverflow < TestType>(0, 5, Result));
+  EXPECT_EQ(Result, TestType(0));
+  EXPECT_FALSE(MulOverflow < TestType>(0, -5, Result));
+  EXPECT_EQ(Result, TestType(0));
 }
 } // namespace

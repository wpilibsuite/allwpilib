load("@rules_cc//cc:cc_test.bzl", "cc_test")

cc_test(
    name = "native",
    size = "small",
    srcs = glob(["cpp/**/*.cpp"]),
    tags = [
        "no-asan",
    ],
    deps = [
        "//apriltag",
        "//thirdparty/catch2",
    ],
)

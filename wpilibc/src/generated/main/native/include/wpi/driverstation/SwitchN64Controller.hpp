// Copyright (c) FIRST and other WPILib contributors.
// Open Source Software; you can modify and/or share it under the terms of
// the WPILib BSD license file in the root directory of this project.

// THIS FILE WAS AUTO-GENERATED BY ./wpilibc/generate_first_ds_hids.py. DO NOT MODIFY
#pragma once

#include "wpi/driverstation/GenericHID.hpp"
#include "wpi/driverstation/HIDDevice.hpp"
#include "wpi/driverstation/TouchpadFinger.hpp"
#include "wpi/telemetry/TelemetryLoggable.hpp"

namespace wpi {

class BooleanEvent;
class EventLoop;

/**
 * Handle input from SwitchN64 controllers connected to the Driver Station.
 *
 * This class handles SwitchN64 input that comes from the Driver Station.
 * Each time a value is requested the most recent value is returned.
 */
class SwitchN64Controller : public HIDDevice, public wpi::TelemetryLoggable {
 public:
  /** The number of touchpads supported by this controller. */
  static constexpr int TOUCHPAD_COUNT = 0;

  /** Whether this controller supports main rumble motors. */
  static constexpr bool SUPPORTS_RUMBLE = false;

  /** Whether this controller supports trigger rumble motors. */
  static constexpr bool SUPPORTS_TRIGGER_RUMBLE = false;

  /** Whether this controller supports mono LED output. */
  static constexpr bool SUPPORTS_MONO_LED = false;

  /** Whether this controller supports RGB LED output. */
  static constexpr bool SUPPORTS_RGB_LED = false;

  /** Supported outputs expected for this controller type. */
  static constexpr GenericHID::SupportedOutputs SUPPORTED_OUTPUTS =
      static_cast<GenericHID::SupportedOutputs>(0);

  /** Represents a digital button on a SwitchN64Controller. */
  enum class Button {
    /// A button.
    A = 0,
    /// B button.
    B = 1,
    /// C Left button.
    C_LEFT = 3,
    /// C Up button.
    C_UP = 4,
    /// Capture button.
    CAPTURE = 15,
    /// Home button.
    HOME = 5,
    /// Start button.
    START = 6,
    /// C Down button.
    C_DOWN = 2,
    /// L button.
    L = 9,
    /// R button.
    R = 10,
    /// Dpad Up button.
    DPAD_UP = 11,
    /// Dpad Down button.
    DPAD_DOWN = 12,
    /// Dpad Left button.
    DPAD_LEFT = 13,
    /// Dpad Right button.
    DPAD_RIGHT = 14,
    /// C Right button.
    C_RIGHT = 21,
  };

  /** Represents an axis on a SwitchN64Controller. */
  enum class Axis {
    /// Left X.
    LEFT_X = 0,
    /// Left Y.
    LEFT_Y = 1,
    /// Z Axis.
    Z_AXIS = 4,
    /// ZR.
    ZR = 5,
  };

  /**
   * Construct an instance of a controller.
   *
   * The controller index is the USB port on the Driver Station.
   *
   * @param port The port on the Driver Station that the controller is plugged
   *             into.
   */
  explicit SwitchN64Controller(int port);

  /**
   * Construct an instance of a controller with a GenericHID object.
   *
   * @param hid The GenericHID object to use for this controller.
   */
  explicit SwitchN64Controller(GenericHID& hid);

  ~SwitchN64Controller() override = default;

  SwitchN64Controller(SwitchN64Controller&&) = default;
  SwitchN64Controller& operator=(SwitchN64Controller&&) = default;

  /**
   * Get the underlying GenericHID object.
   *
   * @return the wrapped GenericHID object
   */
  GenericHID& GetHID() override;

  /**
   * Get the underlying GenericHID object.
   *
   * @return the wrapped GenericHID object
   */
  const GenericHID& GetHID() const override;

  /**
   * Get the port number of the HID.
   *
   * @return The port number of the HID.
   */
  int GetPort() const;

  /**
   * Get if the HID is connected.
   *
   * @return true if the HID is connected
   */
  bool IsConnected() const;

  /**
   * Get the Left X value of the controller.
   *
   * @return the axis value.
   */
  double GetLeftX() const;

  /**
   * Get the Left Y value of the controller.
   *
   * @return the axis value.
   */
  double GetLeftY() const;

  /**
   * Get the Z Axis value of the controller.
   *
   * @return the axis value.
   */
  double GetZAxis() const;

  /**
   * Get the ZR value of the controller.
   *
   * @return the axis value.
   */
  double GetZR() const;

  /**
   * Get the value of the axis.
   *
   * @param axis The axis to read
   * @return the axis value.
   */
  double GetAxis(Axis axis) const;

  /**
   * Constructs an event instance that is true when the axis value is less than
   * threshold.
   *
   * @param axis The axis to read.
   * @param threshold The value below which this event should return true.
   * @param loop the event loop instance to attach the event to.
   * @return an event instance that is true when the axis value is less than the
   * provided threshold.
   */
  BooleanEvent AxisLessThan(Axis axis, double threshold, EventLoop* loop) const;

  /**
   * Constructs an event instance that is true when the axis value is greater
   * than threshold.
   *
   * @param axis The axis to read.
   * @param threshold The value above which this event should return true.
   * @param loop the event loop instance to attach the event to.
   * @return an event instance that is true when the axis value is greater than
   * the provided threshold.
   */
  BooleanEvent AxisGreaterThan(Axis axis, double threshold,
                               EventLoop* loop) const;

  /**
   * Read the value of the A button on the controller.
   *
   * @return The state of the button.
   */
  bool GetAButton() const;

  /**
   * Whether the A button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetAButtonPressed();

  /**
   * Whether the A button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetAButtonReleased();

  /**
   * Constructs an event instance around the A button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the A button's
   * digital signal attached to the given loop.
   */
  BooleanEvent A(EventLoop* loop) const;

  /**
   * Read the value of the B button on the controller.
   *
   * @return The state of the button.
   */
  bool GetBButton() const;

  /**
   * Whether the B button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetBButtonPressed();

  /**
   * Whether the B button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetBButtonReleased();

  /**
   * Constructs an event instance around the B button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the B button's
   * digital signal attached to the given loop.
   */
  BooleanEvent B(EventLoop* loop) const;

  /**
   * Read the value of the C Left button on the controller.
   *
   * @return The state of the button.
   */
  bool GetCLeftButton() const;

  /**
   * Whether the C Left button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetCLeftButtonPressed();

  /**
   * Whether the C Left button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetCLeftButtonReleased();

  /**
   * Constructs an event instance around the C Left button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the C Left button's
   * digital signal attached to the given loop.
   */
  BooleanEvent CLeft(EventLoop* loop) const;

  /**
   * Read the value of the C Up button on the controller.
   *
   * @return The state of the button.
   */
  bool GetCUpButton() const;

  /**
   * Whether the C Up button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetCUpButtonPressed();

  /**
   * Whether the C Up button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetCUpButtonReleased();

  /**
   * Constructs an event instance around the C Up button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the C Up button's
   * digital signal attached to the given loop.
   */
  BooleanEvent CUp(EventLoop* loop) const;

  /**
   * Read the value of the Capture button on the controller.
   *
   * @return The state of the button.
   */
  bool GetCaptureButton() const;

  /**
   * Whether the Capture button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetCaptureButtonPressed();

  /**
   * Whether the Capture button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetCaptureButtonReleased();

  /**
   * Constructs an event instance around the Capture button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the Capture button's
   * digital signal attached to the given loop.
   */
  BooleanEvent Capture(EventLoop* loop) const;

  /**
   * Read the value of the Home button on the controller.
   *
   * @return The state of the button.
   */
  bool GetHomeButton() const;

  /**
   * Whether the Home button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetHomeButtonPressed();

  /**
   * Whether the Home button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetHomeButtonReleased();

  /**
   * Constructs an event instance around the Home button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the Home button's
   * digital signal attached to the given loop.
   */
  BooleanEvent Home(EventLoop* loop) const;

  /**
   * Read the value of the Start button on the controller.
   *
   * @return The state of the button.
   */
  bool GetStartButton() const;

  /**
   * Whether the Start button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetStartButtonPressed();

  /**
   * Whether the Start button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetStartButtonReleased();

  /**
   * Constructs an event instance around the Start button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the Start button's
   * digital signal attached to the given loop.
   */
  BooleanEvent Start(EventLoop* loop) const;

  /**
   * Read the value of the C Down button on the controller.
   *
   * @return The state of the button.
   */
  bool GetCDownButton() const;

  /**
   * Whether the C Down button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetCDownButtonPressed();

  /**
   * Whether the C Down button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetCDownButtonReleased();

  /**
   * Constructs an event instance around the C Down button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the C Down button's
   * digital signal attached to the given loop.
   */
  BooleanEvent CDown(EventLoop* loop) const;

  /**
   * Read the value of the L button on the controller.
   *
   * @return The state of the button.
   */
  bool GetLButton() const;

  /**
   * Whether the L button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetLButtonPressed();

  /**
   * Whether the L button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetLButtonReleased();

  /**
   * Constructs an event instance around the L button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the L button's
   * digital signal attached to the given loop.
   */
  BooleanEvent L(EventLoop* loop) const;

  /**
   * Read the value of the R button on the controller.
   *
   * @return The state of the button.
   */
  bool GetRButton() const;

  /**
   * Whether the R button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetRButtonPressed();

  /**
   * Whether the R button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetRButtonReleased();

  /**
   * Constructs an event instance around the R button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the R button's
   * digital signal attached to the given loop.
   */
  BooleanEvent R(EventLoop* loop) const;

  /**
   * Read the value of the Dpad Up button on the controller.
   *
   * @return The state of the button.
   */
  bool GetDpadUpButton() const;

  /**
   * Whether the Dpad Up button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetDpadUpButtonPressed();

  /**
   * Whether the Dpad Up button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetDpadUpButtonReleased();

  /**
   * Constructs an event instance around the Dpad Up button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the Dpad Up button's
   * digital signal attached to the given loop.
   */
  BooleanEvent DpadUp(EventLoop* loop) const;

  /**
   * Read the value of the Dpad Down button on the controller.
   *
   * @return The state of the button.
   */
  bool GetDpadDownButton() const;

  /**
   * Whether the Dpad Down button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetDpadDownButtonPressed();

  /**
   * Whether the Dpad Down button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetDpadDownButtonReleased();

  /**
   * Constructs an event instance around the Dpad Down button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the Dpad Down button's
   * digital signal attached to the given loop.
   */
  BooleanEvent DpadDown(EventLoop* loop) const;

  /**
   * Read the value of the Dpad Left button on the controller.
   *
   * @return The state of the button.
   */
  bool GetDpadLeftButton() const;

  /**
   * Whether the Dpad Left button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetDpadLeftButtonPressed();

  /**
   * Whether the Dpad Left button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetDpadLeftButtonReleased();

  /**
   * Constructs an event instance around the Dpad Left button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the Dpad Left button's
   * digital signal attached to the given loop.
   */
  BooleanEvent DpadLeft(EventLoop* loop) const;

  /**
   * Read the value of the Dpad Right button on the controller.
   *
   * @return The state of the button.
   */
  bool GetDpadRightButton() const;

  /**
   * Whether the Dpad Right button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetDpadRightButtonPressed();

  /**
   * Whether the Dpad Right button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetDpadRightButtonReleased();

  /**
   * Constructs an event instance around the Dpad Right button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the Dpad Right button's
   * digital signal attached to the given loop.
   */
  BooleanEvent DpadRight(EventLoop* loop) const;

  /**
   * Read the value of the C Right button on the controller.
   *
   * @return The state of the button.
   */
  bool GetCRightButton() const;

  /**
   * Whether the C Right button was pressed since the last check.
   *
   * @return Whether the button was pressed since the last check.
   */
  bool GetCRightButtonPressed();

  /**
   * Whether the C Right button was released since the last check.
   *
   * @return Whether the button was released since the last check.
   */
  bool GetCRightButtonReleased();

  /**
   * Constructs an event instance around the C Right button's
   * digital signal.
   *
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the C Right button's
   * digital signal attached to the given loop.
   */
  BooleanEvent CRight(EventLoop* loop) const;

  /**
   * Get the button value.
   *
   * @param button The button to read
   * @return The state of the button.
   */
  bool GetButton(Button button) const;

  /**
   * Whether the button was pressed since the last check.
   *
   * @param button The button to read
   * @return Whether the button was pressed since the last check.
   */
  bool GetButtonPressed(Button button);

  /**
   * Whether the button was released since the last check.
   *
   * @param button The button to read
   * @return Whether the button was released since the last check.
   */
  bool GetButtonReleased(Button button);

  /**
   * Constructs an event instance around this button's digital signal.
   *
   * @param button the button
   * @param loop the event loop instance to attach the event to.
   * @return an event instance representing the button's digital signal attached
   * to the given loop.
   */
  BooleanEvent ButtonEvent(Button button, EventLoop* loop) const;

  /**
   * Set leds on the controller.
   *
   * @param r Red value from 0-255
   * @param g Green value from 0-255
   * @param b Blue value from 0-255
   */
  void SetLeds(int r, int g, int b);

  /**
   * Set the rumble output for the HID.
   *
   * @param type Which rumble value to set
   * @param value The normalized value (0 to 1) to set the rumble to
   */
  void SetRumble(GenericHID::RumbleType type, double value);

  std::string_view GetTelemetryType() const override;
  void LogTo(wpi::TelemetryTable& table) const override;

 private:
  GenericHID* m_hid;
};

}  // namespace wpi

extra_includes:
- wpi/util/sendable/SendableBuilder.hpp

classes:
  wpi::Solenoid:
    ignored_bases:
    - wpi::util::SendableHelper<Solenoid>
    methods:
      Solenoid:
        overloads:
          CANBusMap, PneumaticsModuleType, int:
          CANBusMap, int, PneumaticsModuleType, int:
      Set:
      Get:
      Toggle:
      GetChannel:
      IsDisabled:
      SetPulseDuration:
      StartPulse:
      InitSendable:

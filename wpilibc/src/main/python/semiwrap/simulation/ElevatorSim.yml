classes:
  wpi::sim::ElevatorSim:
    typealias:
    - wpi::math::DCMotor
    - template <int S, int I, int O> using LinearSystem = wpi::math::LinearSystem<S, I, O>
    - template <int I> using Vectord = wpi::math::Vectord<I>
    methods:
      ElevatorSim:
        overloads:
          ? const wpi::math::LinearSystem<2, 1, 2>&, const wpi::math::DCMotor&, wpi::units::meters<>, wpi::units::meters<>, bool, wpi::units::meters<>, const std::array<double, 2>&
          : param_override:
              measurementStdDevs:
                default: std::array<double, 2>{0.0, 0.0}
          ? const wpi::math::DCMotor&, double, wpi::units::kilograms<>, wpi::units::meters<>, wpi::units::meters<>, wpi::units::meters<>, bool, wpi::units::meters<>, const std::array<double, 2>&
          : param_override:
              measurementStdDevs:
                default: std::array<double, 2>{0.0}
          ? decltype(1_V/Velocity_t<Distance> (1)), decltype(1_V/Acceleration_t<Distance> (1)), const wpi::math::DCMotor&, wpi::units::meters<>, wpi::units::meters<>, bool, wpi::units::meters<>, const std::array<double, 2>&
          : ignore: true
      SetState:
      WouldHitLowerLimit:
      WouldHitUpperLimit:
      HasHitLowerLimit:
      HasHitUpperLimit:
      GetPosition:
      GetVelocity:
      GetCurrentDraw:
      SetInputVoltage:
      UpdateX:

inline_code: |-
  cls_ElevatorSim
    .def("get_position_feet", [](ElevatorSim * self) -> wpi::units::feet<> {
      return self->GetPosition();
    })
    .def("get_position_inches", [](ElevatorSim * self) -> wpi::units::inches<> {
      return self->GetPosition();
    })
    .def("get_velocity_fps", [](ElevatorSim * self) -> wpi::units::feet_per_second<> {
      return self->GetVelocity();
    })
  ;

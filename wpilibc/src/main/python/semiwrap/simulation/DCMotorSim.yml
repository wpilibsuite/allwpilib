classes:
  wpi::sim::DCMotorSim:
    typealias:
    - wpi::math::DCMotor
    methods:
      DCMotorSim:
        overloads:
          const wpi::math::LinearSystem<2, 1, 2>&, const wpi::math::DCMotor&, const std::array<double, 2>&:
            param_override:
              plant:
                x_type: wpi::math::LinearSystem<2,1,2>
      SetState:
      GetAngularPosition:
      GetAngularVelocity:
      GetCurrentDraw:
      SetInputVoltage:
      GetAngularAcceleration:
      GetTorque:
      GetInputVoltage:
      GetGearbox:
      GetGearing:
      GetJ:
      SetAngle:
      SetAngularVelocity:

inline_code: |
  cls_DCMotorSim
    // java API compatibility
    .def("get_angular_position_rotations", [](const DCMotorSim &self) {
      return wpi::units::turn_t{self.GetAngularPosition()};
    }, py::doc("Returns the DC motor position in rotations"))
    .def("get_angular_velocity_rpm", [](const DCMotorSim &self) {
      return wpi::units::revolutions_per_minute_t{self.GetAngularVelocity()};
    }, py::doc("Returns the DC motor velocity in revolutions per minute"))
  ;

extra_includes:
- wpi/util/sendable/SendableBuilder.hpp
- wpi/hardware/rotation/DutyCycleInput.hpp

classes:
  wpi::DutyCycleEncoder:
    ignored_bases:
    - wpi::util::SendableHelper<DutyCycleEncoder>
    methods:
      DutyCycleEncoder:
        overloads:
          int:
          DutyCycleInput&:
            ignore: true
          DutyCycleInput*:
            ignore: true
          std::shared_ptr<DutyCycleInput>:
          int, double, double:
          DutyCycleInput&, double, double:
            ignore: true
          DutyCycleInput*, double, double:
            ignore: true
          std::shared_ptr<DutyCycleInput>, double, double:
      GetFrequency:
      IsConnected:
      SetConnectedFrequencyThreshold:
      Get:
      SetDutyCycleRange:
      GetSourceChannel:
      SetAssumedFrequency:
      SetInverted:
      InitSendable:

inline_code: |
  cls_DutyCycleEncoder
    .def("__repr__", [](const DutyCycleEncoder &self) {
      return py::str("<DutyCycleEncoder {}>").format(self.GetSourceChannel());
    });

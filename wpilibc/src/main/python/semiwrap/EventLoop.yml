classes:
  wpi::EventLoop:
    force_type_casters:
    - std::function
    methods:
      EventLoop:
      Bind:
        cpp_code: |
          [](EventLoop *self, std::function<void()> action) {
            return self->Bind(std::move(action));
          }
      Unbind:
      Poll:
      Clear:

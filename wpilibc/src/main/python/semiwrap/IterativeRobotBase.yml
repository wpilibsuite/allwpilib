classes:
  wpi::IterativeRobotBase:
    methods:
      DriverStationConnected:
      DisabledEnter:
      AutonomousEnter:
      TeleopEnter:
      UtilityEnter:
      RobotPeriodic:
      DisabledPeriodic:
      AutonomousPeriodic:
      TeleopPeriodic:
      UtilityPeriodic:
      IterativeRobotBase:
        overloads:
          double:
            ignore: true
          wpi::units::second_t:
      LoopFunc:
      SimulationInit:
      SimulationPeriodic:
      DisabledExit:
      AutonomousExit:
      TeleopExit:
      UtilityExit:
      GetPeriod:
      PrintWatchdogEpochs:
    doc: |
      IterativeRobotBase implements a specific type of robot program framework,
      extending the RobotBase class.

      The IterativeRobotBase class does not implement StartCompetition(), so it
      should not be used by teams directly.

      This class provides the following functions which are called by the main
      loop, StartCompetition(), at the appropriate times:

      DriverStationConnected() -- provide for initialization the first time the DS
      is connected

      Enter() functions -- each of the following functions is called once when the
      appropriate mode is entered:

      - DisabledEnter() -- called each and every time disabled is entered from another mode
      - AutonomousEnter() -- called each and every time autonomous is entered from another mode
      - TeleopEnter() -- called each and every time teleop is entered from another mode
      - UtilityEnter() -- called each and every time utility is entered from another mode

      Periodic() functions -- each of these functions is called on an interval:

      - RobotPeriodic()
      - DisabledPeriodic()
      - AutonomousPeriodic()
      - TeleopPeriodic()
      - UtilityPeriodic()

      Exit() functions -- each of the following functions is called once when the
      appropriate mode is exited:

      - DisabledExit() -- called each and every time disabled is exited
      - AutonomousExit() -- called each and every time autonomous is exited
      - TeleopExit() -- called each and every time teleop is exited
      - UtilityExit() -- called each and every time utility is exited

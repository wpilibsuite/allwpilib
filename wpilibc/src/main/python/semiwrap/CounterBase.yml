defaults:
  subpackage: interfaces

classes:
  wpi::CounterBase:
    enums:
      EncodingType:
    methods:
      CounterBase:
      Get:
      Reset:
      GetPeriod:
      SetMaxPeriod:
      GetStopped:
      GetDirection:

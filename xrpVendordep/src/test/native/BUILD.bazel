load("@rules_cc//cc:cc_test.bzl", "cc_test")

cc_test(
    name = "native",
    size = "small",
    srcs = glob(["cpp/**"]),
    deps = [
        "//thirdparty/catch2",
        "//xrpVendordep",
    ],
)

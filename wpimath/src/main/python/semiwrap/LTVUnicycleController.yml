classes:
  wpi::math::LTVUnicycleController:
    methods:
      LTVUnicycleController:
        overloads:
          wpi::units::seconds<>:
          const wpi::util::array<double, 3>&, const wpi::util::array<double, 2>&, wpi::units::seconds<>:
            # param_override:
            #   maxVelocity:
            #     default: 9_mps
      AtReference:
      SetTolerance:
      Calculate:
        overloads:
          const Pose2d&, const Pose2d&, wpi::units::meters_per_second<>, wpi::units::radians_per_second<>:
          const Pose2d&, const HolonomicSample&:
      SetEnabled:

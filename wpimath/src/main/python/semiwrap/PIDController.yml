extra_includes:
- wpi/tunable/TunableTable.hpp

classes:
  wpi::math::PIDController:
    ignored_bases:
    - wpi::TelemetryLoggable  # TODO(telemetry PR)
    - wpi::ComplexTunable  # TODO(telemetry PR)
    methods:
      PIDController:
        param_override:
          period:
            default: 0.020_s
      SetPID:
      SetP:
      SetI:
      SetD:
      SetIZone:
      GetP:
      GetI:
      GetD:
      GetIZone:
      GetPeriod:
      GetErrorTolerance:
      GetErrorDerivativeTolerance:
      GetAccumulatedError:
      SetSetpoint:
      GetSetpoint:
      AtSetpoint:
      EnableContinuousInput:
      DisableContinuousInput:
      IsContinuousInputEnabled:
      SetIntegratorRange:
      SetTolerance:
      GetError:
      GetErrorDerivative:
      Calculate:
        overloads:
          double:
          double, double:
      Reset:
      LogTo:
      GetTelemetryType:
      PublishTunable:
      GetTunableType:

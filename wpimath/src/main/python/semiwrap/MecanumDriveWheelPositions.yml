extra_includes:
- wpystruct.h

classes:
  wpi::math::MecanumDriveWheelPositions:
    force_no_default_constructor: true
    attributes:
      frontLeft:
      frontRight:
      rearLeft:
      rearRight:
    methods:
      operator==:
      Interpolate:
    inline_code: |
      .def(
        py::init<
          wpi::units::meter_t, wpi::units::meter_t, wpi::units::meter_t, wpi::units::meter_t
        >(),
        py::arg("front_left") = 0, py::arg("front_right") = 0, py::arg("rear_left") = 0, py::arg("rear_right") = 0
      )

inline_code: |
  SetupWPyStruct<wpi::math::MecanumDriveWheelPositions>(cls_MecanumDriveWheelPositions);

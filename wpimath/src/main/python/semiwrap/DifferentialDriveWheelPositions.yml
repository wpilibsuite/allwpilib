extra_includes:
- wpystruct.h

classes:
  wpi::math::DifferentialDriveWheelPositions:
    attributes:
      left:
      right:
    methods:
      operator==:
      Interpolate:

inline_code: |
  SetupWPyStruct<wpi::math::DifferentialDriveWheelPositions>(cls_DifferentialDriveWheelPositions);

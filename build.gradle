apply plugin: 'cpp'

model {
    components {
        NetworkTables(NativeLibrarySpec) {
            targetPlatform 'arm'
            sources {
                cpp {
                    source {
                        srcDirs = ["src"]
                        includes = ["**/*.c"]
                    }
                    exportedHeaders {
                        srcDirs = ["include"]
                    }
                }
            }
        }
    }
}

